[package]
name = "azure_ml"
version = "0.1.0"
edition = "2021"
rust-version = "1.83"

[features]
default = ["azure_core/default"]

[dependencies]
async-trait = "0.1"
azure_core = "0.32"
serde = { version = "1.0", features = ["derive"] }

// Copyright (c) Microsoft Corporation. All rights reserved.
// Licensed under the MIT License.
//
// Code generated by Microsoft (R) Rust Code Generator. DO NOT EDIT.

/// Extensible enum. Indicates the action type. "Internal" refers to actions that are for internal only APIs.
#[derive(Clone, Debug, Eq, PartialEq)]
pub enum ActionType {
    /// Actions are for internal-only APIs.
    Internal,

    /// Any other value not defined in `ActionType`.
    UnknownValue(String),
}

/// Allocation state of the compute. Possible values are: steady - Indicates that the compute is not resizing. There are no
/// changes to the number of compute nodes in the compute in progress. A compute enters this state when it is created and
/// when no operations are being performed on the compute to change the number of compute nodes. resizing - Indicates that
/// the compute is resizing; that is, compute nodes are being added to or removed from the compute.
#[derive(Clone, Debug, Eq, PartialEq)]
pub enum AllocationState {
    Resizing,

    Steady,

    /// Any other value not defined in `AllocationState`.
    UnknownValue(String),
}

/// Policy for sharing applications on this compute instance among users of parent workspace. If Personal, only the creator
/// can access applications on this compute instance. When Shared, any workspace user can access applications on this instance
/// depending on his/her assigned role.
#[derive(Clone, Debug, Eq, PartialEq)]
pub enum ApplicationSharingPolicy {
    Personal,

    Shared,

    /// Any other value not defined in `ApplicationSharingPolicy`.
    UnknownValue(String),
}

/// Provisioning state of registry asset.
#[derive(Clone, Debug, Eq, PartialEq)]
pub enum AssetProvisioningState {
    Canceled,

    Creating,

    Deleting,

    Failed,

    Succeeded,

    Updating,

    /// Any other value not defined in `AssetProvisioningState`.
    UnknownValue(String),
}

/// AutoRebuild setting for the derived image
#[derive(Clone, Debug, Eq, PartialEq)]
pub enum AutoRebuildSetting {
    Disabled,

    OnBaseImageUpdate,

    /// Any other value not defined in `AutoRebuildSetting`.
    UnknownValue(String),
}

/// Auto save settings.
#[derive(Clone, Debug, Eq, PartialEq)]
pub enum Autosave {
    Local,

    None,

    Remote,

    /// Any other value not defined in `Autosave`.
    UnknownValue(String),
}

/// The enumerated property types for batch deployments.
#[derive(Clone, Debug, Eq, PartialEq)]
pub enum BatchDeploymentConfigurationType {
    Model,

    PipelineComponent,

    /// Any other value not defined in `BatchDeploymentConfigurationType`.
    UnknownValue(String),
}

/// Log verbosity for batch inferencing.
/// Increasing verbosity order for logging is : Warning, Info and Debug.
/// The default value is Info.
#[derive(Clone, Debug, Eq, PartialEq)]
pub enum BatchLoggingLevel {
    Debug,

    Info,

    Warning,

    /// Any other value not defined in `BatchLoggingLevel`.
    UnknownValue(String),
}

/// Enum to determine how batch inferencing will handle output
#[derive(Clone, Debug, Eq, PartialEq)]
pub enum BatchOutputAction {
    AppendRow,

    SummaryOnly,

    /// Any other value not defined in `BatchOutputAction`.
    UnknownValue(String),
}

/// Three lettered code specifying the currency of the VM price. Example: USD
#[derive(Clone, Debug, Eq, PartialEq)]
pub enum BillingCurrency {
    Usd,

    /// Any other value not defined in `BillingCurrency`.
    UnknownValue(String),
}

/// Enum for all classification models supported by AutoML.
#[derive(Clone, Debug, Eq, PartialEq)]
pub enum BlockedTransformers {
    /// Target encoding for categorical data.
    CatTargetEncoder,

    /// Count Vectorizer converts a collection of text documents to a matrix of token counts.
    CountVectorizer,

    /// Hashing One Hot Encoder can turn categorical variables into a limited number of new features. This is often used for high-cardinality
    /// categorical features.
    HashOneHotEncoder,

    /// Label encoder converts labels/categorical variables in a numerical form.
    LabelEncoder,

    /// Naive Bayes is a classified that is used for classification of discrete features that are categorically distributed.
    NaiveBayes,

    /// Ohe hot encoding creates a binary feature transformation.
    OneHotEncoder,

    /// Target encoding for text data.
    TextTargetEncoder,

    /// Tf-Idf stands for, term-frequency times inverse document-frequency. This is a common term weighting scheme for identifying
    /// information from documents.
    TfIdf,

    /// Weight of Evidence encoding is a technique used to encode categorical variables. It uses the natural log of the P(1)/P(0)
    /// to create weights.
    WoETargetEncoder,

    /// Word embedding helps represents words or phrases as a vector, or a series of numbers.
    WordEmbedding,

    /// Any other value not defined in `BlockedTransformers`.
    UnknownValue(String),
}

/// Caching type of Data Disk.
#[derive(Clone, Debug, Eq, PartialEq)]
pub enum Caching {
    None,

    ReadOnly,

    ReadWrite,

    /// Any other value not defined in `Caching`.
    UnknownValue(String),
}

#[derive(Clone, Debug, Eq, PartialEq)]
pub enum CapabilityHostKind {
    Agents,

    /// Any other value not defined in `CapabilityHostKind`.
    UnknownValue(String),
}

/// Provisioning state of capability host.
#[derive(Clone, Debug, Eq, PartialEq)]
pub enum CapabilityHostProvisioningState {
    Canceled,

    Creating,

    Deleting,

    Failed,

    Succeeded,

    Updating,

    /// Any other value not defined in `CapabilityHostProvisioningState`.
    UnknownValue(String),
}

#[derive(Clone, Debug, Eq, PartialEq)]
pub enum CategoricalDataDriftMetric {
    /// The Jensen Shannon Distance (JSD) metric.
    JensenShannonDistance,

    /// The Pearsons Chi Squared Test metric.
    PearsonsChiSquaredTest,

    /// The Population Stability Index (PSI) metric.
    PopulationStabilityIndex,

    /// Any other value not defined in `CategoricalDataDriftMetric`.
    UnknownValue(String),
}

#[derive(Clone, Debug, Eq, PartialEq)]
pub enum CategoricalDataQualityMetric {
    /// Calculates the rate of data type errors.
    DataTypeErrorRate,

    /// Calculates the rate of null values.
    NullValueRate,

    /// Calculates the rate values are out of bounds.
    OutOfBoundsRate,

    /// Any other value not defined in `CategoricalDataQualityMetric`.
    UnknownValue(String),
}

#[derive(Clone, Debug, Eq, PartialEq)]
pub enum CategoricalPredictionDriftMetric {
    /// The Jensen Shannon Distance (JSD) metric.
    JensenShannonDistance,

    /// The Pearsons Chi Squared Test metric.
    PearsonsChiSquaredTest,

    /// The Population Stability Index (PSI) metric.
    PopulationStabilityIndex,

    /// Any other value not defined in `CategoricalPredictionDriftMetric`.
    UnknownValue(String),
}

/// Enum for all classification models supported by AutoML.
#[derive(Clone, Debug, Eq, PartialEq)]
pub enum ClassificationModels {
    /// Naive Bayes classifier for multivariate Bernoulli models.
    BernoulliNaiveBayes,

    /// Decision Trees are a non-parametric supervised learning method used for both classification and regression tasks.
    /// The goal is to create a model that predicts the value of a target variable by learning simple decision rules inferred
    /// from the data features.
    DecisionTree,

    /// Extreme Trees is an ensemble machine learning algorithm that combines the predictions from many decision trees. It is
    /// related to the widely used random forest algorithm.
    ExtremeRandomTrees,

    /// The technique of transiting week learners into a strong learner is called Boosting. The gradient boosting algorithm process
    /// works on this theory of execution.
    GradientBoosting,

    /// K-nearest neighbors (KNN) algorithm uses 'feature similarity' to predict the values of new datapoints
    /// which further means that the new data point will be assigned a value based on how closely it matches the points in the
    /// training set.
    Knn,

    /// LightGBM is a gradient boosting framework that uses tree based learning algorithms.
    LightGbm,

    /// A support vector machine (SVM) is a supervised machine learning model that uses classification algorithms for two-group
    /// classification problems.
    /// After giving an SVM model sets of labeled training data for each category, they're able to categorize new text.
    /// Linear SVM performs best when input data is linear, i.e., data can be easily classified by drawing the straight line between
    /// classified values on a plotted graph.
    LinearSvm,

    /// Logistic regression is a fundamental classification technique.
    /// It belongs to the group of linear classifiers and is somewhat similar to polynomial and linear regression.
    /// Logistic regression is fast and relatively uncomplicated, and it's convenient for you to interpret the results.
    /// Although it's essentially a method for binary classification, it can also be applied to multiclass problems.
    LogisticRegression,

    /// The multinomial Naive Bayes classifier is suitable for classification with discrete features (e.g., word counts for text
    /// classification).
    /// The multinomial distribution normally requires integer feature counts. However, in practice, fractional counts such as
    /// tf-idf may also work.
    MultinomialNaiveBayes,

    /// Random forest is a supervised learning algorithm.
    /// The "forest" it builds, is an ensemble of decision trees, usually trained with the "bagging" method.
    /// The general idea of the bagging method is that a combination of learning models increases the overall result.
    RandomForest,

    /// SGD: Stochastic gradient descent is an optimization algorithm often used in machine learning applications
    /// to find the model parameters that correspond to the best fit between predicted and actual outputs.
    Sgd,

    /// A support vector machine (SVM) is a supervised machine learning model that uses classification algorithms for two-group
    /// classification problems.
    /// After giving an SVM model sets of labeled training data for each category, they're able to categorize new text.
    Svm,

    /// XGBoost: Extreme Gradient Boosting Algorithm. This algorithm is used for structured data where target column values can
    /// be divided into distinct class values.
    XgBoostClassifier,

    /// Any other value not defined in `ClassificationModels`.
    UnknownValue(String),
}

/// Primary metrics for classification multilabel tasks.
#[derive(Clone, Debug, Eq, PartialEq)]
pub enum ClassificationMultilabelPrimaryMetrics {
    /// Accuracy is the ratio of predictions that exactly match the true class labels.
    Accuracy,

    /// AUC is the Area under the curve.
    /// This metric represents arithmetic mean of the score for each class,
    /// weighted by the number of true instances in each class.
    AucWeighted,

    /// The arithmetic mean of the average precision score for each class, weighted by
    /// the number of true instances in each class.
    AveragePrecisionScoreWeighted,

    /// Intersection Over Union. Intersection of predictions divided by union of predictions.
    Iou,

    /// Normalized macro recall is recall macro-averaged and normalized, so that random
    /// performance has a score of 0, and perfect performance has a score of 1.
    NormMacroRecall,

    /// The arithmetic mean of precision for each class, weighted by number of true instances in each class.
    PrecisionScoreWeighted,

    /// Any other value not defined in `ClassificationMultilabelPrimaryMetrics`.
    UnknownValue(String),
}

/// Primary metrics for classification tasks.
#[derive(Clone, Debug, Eq, PartialEq)]
pub enum ClassificationPrimaryMetrics {
    /// Accuracy is the ratio of predictions that exactly match the true class labels.
    Accuracy,

    /// AUC is the Area under the curve.
    /// This metric represents arithmetic mean of the score for each class,
    /// weighted by the number of true instances in each class.
    AucWeighted,

    /// The arithmetic mean of the average precision score for each class, weighted by
    /// the number of true instances in each class.
    AveragePrecisionScoreWeighted,

    /// Normalized macro recall is recall macro-averaged and normalized, so that random
    /// performance has a score of 0, and perfect performance has a score of 1.
    NormMacroRecall,

    /// The arithmetic mean of precision for each class, weighted by number of true instances in each class.
    PrecisionScoreWeighted,

    /// Any other value not defined in `ClassificationPrimaryMetrics`.
    UnknownValue(String),
}

/// Intended usage of the cluster
#[derive(Clone, Debug, Eq, PartialEq)]
pub enum ClusterPurpose {
    DenseProd,

    DevTest,

    FastProd,

    /// Any other value not defined in `ClusterPurpose`.
    UnknownValue(String),
}

/// The Compute Instance Authorization type. Available values are personal (default).
#[derive(Clone, Debug, Eq, PartialEq)]
pub enum ComputeInstanceAuthorizationType {
    Personal,

    /// Any other value not defined in `ComputeInstanceAuthorizationType`.
    UnknownValue(String),
}

/// Current state of an ComputeInstance.
#[derive(Clone, Debug, Eq, PartialEq)]
pub enum ComputeInstanceState {
    CreateFailed,

    Creating,

    Deleting,

    JobRunning,

    Resizing,

    Restarting,

    Running,

    SettingUp,

    SetupFailed,

    Starting,

    Stopped,

    Stopping,

    Unknown,

    Unusable,

    UserSettingUp,

    UserSetupFailed,

    /// Any other value not defined in `ComputeInstanceState`.
    UnknownValue(String),
}

/// [Required] The compute power action.
#[derive(Clone, Debug, Eq, PartialEq)]
pub enum ComputePowerAction {
    Start,

    Stop,

    /// Any other value not defined in `ComputePowerAction`.
    UnknownValue(String),
}

/// Enum to describe the frequency of a compute recurrence schedule
#[derive(Clone, Debug, Eq, PartialEq)]
pub enum ComputeRecurrenceFrequency {
    /// Day frequency
    Day,

    /// Hour frequency
    Hour,

    /// Minute frequency
    Minute,

    /// Month frequency
    Month,

    /// Week frequency
    Week,

    /// Any other value not defined in `ComputeRecurrenceFrequency`.
    UnknownValue(String),
}

#[derive(Clone, Debug, Eq, PartialEq)]
pub enum ComputeTriggerType {
    Cron,

    Recurrence,

    /// Any other value not defined in `ComputeTriggerType`.
    UnknownValue(String),
}

/// The type of compute
#[derive(Clone, Debug, Eq, PartialEq)]
pub enum ComputeType {
    Aks,

    AmlCompute,

    ComputeInstance,

    DataFactory,

    DataLakeAnalytics,

    Databricks,

    HdInsight,

    Kubernetes,

    SynapseSpark,

    VirtualMachine,

    /// Any other value not defined in `ComputeType`.
    UnknownValue(String),
}

/// Enum of weekday
#[derive(Clone, Debug, Eq, PartialEq)]
pub enum ComputeWeekDay {
    /// Friday weekday
    Friday,

    /// Monday weekday
    Monday,

    /// Saturday weekday
    Saturday,

    /// Sunday weekday
    Sunday,

    /// Thursday weekday
    Thursday,

    /// Tuesday weekday
    Tuesday,

    /// Wednesday weekday
    Wednesday,

    /// Any other value not defined in `ComputeWeekDay`.
    UnknownValue(String),
}

/// Authentication type of the connection target
#[derive(Clone, Debug, Eq, PartialEq)]
pub enum ConnectionAuthType {
    Aad,

    AccessKey,

    AccountKey,

    AccountManagedIdentity,

    AgentUserImpersonation,

    AgenticIdentityToken,

    AgenticUser,

    ApiKey,

    CustomKeys,

    DelegatedSas,

    ManagedIdentity,

    None,

    OAuth2,

    Pat,

    ProjectManagedIdentity,

    Sas,

    ServicePrincipal,

    UserEntraToken,

    UsernamePassword,

    /// Any other value not defined in `ConnectionAuthType`.
    UnknownValue(String),
}

/// Category of the connection
#[derive(Clone, Debug, Eq, PartialEq)]
pub enum ConnectionCategory {
    AdlsGen2,

    AiServices,

    AmazonMws,

    AmazonRdsForOracle,

    AmazonRdsForSqlServer,

    AmazonRedshift,

    AmazonS3Compatible,

    ApiKey,

    ApiManagement,

    AppConfig,

    AppInsights,

    AzureBlob,

    AzureContainerAppEnvironment,

    AzureDataExplorer,

    AzureDatabricksDeltaLake,

    AzureKeyVault,

    AzureMariaDb,

    AzureMySqlDb,

    AzureOneLake,

    AzureOpenAi,

    AzurePostgresDb,

    AzureSqlDb,

    AzureSqlMi,

    AzureStorageAccount,

    AzureSynapseAnalytics,

    AzureTableStorage,

    BingLlmSearch,

    Cassandra,

    CognitiveSearch,

    CognitiveService,

    Concur,

    ContainerRegistry,

    CosmosDb,

    CosmosDbMongoDbApi,

    Couchbase,

    CustomKeys,

    Databricks,

    Db2,

    Drill,

    Dynamics,

    DynamicsAx,

    DynamicsCrm,

    Elasticsearch,

    Eloqua,

    FileServer,

    FtpServer,

    GenericContainerRegistry,

    GenericHttp,

    GenericRest,

    Git,

    GoogleAdWords,

    GoogleBigQuery,

    GoogleCloudStorage,

    Greenplum,

    GroundingWithBingSearch,

    GroundingWithCustomSearch,

    Hbase,

    Hdfs,

    Hive,

    Hubspot,

    Impala,

    Informix,

    Jira,

    Magento,

    ManagedOnlineEndpoint,

    MariaDb,

    Marketo,

    MicrosoftAccess,

    MicrosoftFabric,

    ModelGateway,

    MongoDbAtlas,

    MongoDbV2,

    MySql,

    Netezza,

    ODataRest,

    Odbc,

    Office365,

    OpenAi,

    Oracle,

    OracleCloudStorage,

    OracleServiceCloud,

    PayPal,

    Phoenix,

    Pinecone,

    PostgreSql,

    PowerPlatformEnvironment,

    Presto,

    PythonFeed,

    QuickBooks,

    Redis,

    RemoteA2A,

    RemoteTool,

    Responsys,

    S3,

    Salesforce,

    SalesforceMarketingCloud,

    SalesforceServiceCloud,

    SapBw,

    SapCloudForCustomer,

    SapEcc,

    SapHana,

    SapOpenHub,

    SapTable,

    Serp,

    Serverless,

    ServiceNow,

    Sftp,

    SharePointOnlineList,

    Sharepoint,

    Shopify,

    Snowflake,

    Spark,

    SqlServer,

    Square,

    Sybase,

    Teradata,

    Vertica,

    WebTable,

    Xero,

    Zoho,

    /// Any other value not defined in `ConnectionCategory`.
    UnknownValue(String),
}

/// Group based on connection category
#[derive(Clone, Debug, Eq, PartialEq)]
pub enum ConnectionGroup {
    Azure,

    AzureAi,

    Database,

    File,

    GenericProtocol,

    NoSql,

    ServicesAndApps,

    /// Any other value not defined in `ConnectionGroup`.
    UnknownValue(String),
}

#[derive(Clone, Debug, Eq, PartialEq)]
pub enum ContainerType {
    InferenceServer,

    StorageInitializer,

    /// Any other value not defined in `ContainerType`.
    UnknownValue(String),
}

/// Specifies the status of content safety.
#[derive(Clone, Debug, Eq, PartialEq)]
pub enum ContentSafetyStatus {
    Disabled,

    Enabled,

    /// Any other value not defined in `ContentSafetyStatus`.
    UnknownValue(String),
}

/// The kind of entity that created the resource.
#[derive(Clone, Debug, Eq, PartialEq)]
pub enum CreatedByType {
    /// The entity was created by an application.
    Application,

    /// The entity was created by a key.
    Key,

    /// The entity was created by a managed identity.
    ManagedIdentity,

    /// The entity was created by a user.
    User,

    /// Any other value not defined in `CreatedByType`.
    UnknownValue(String),
}

/// Enum to determine the datastore credentials type.
#[derive(Clone, Debug, Eq, PartialEq)]
pub enum CredentialsType {
    AccountKey,

    Certificate,

    None,

    Sas,

    ServicePrincipal,

    /// Any other value not defined in `CredentialsType`.
    UnknownValue(String),
}

#[derive(Clone, Debug, Eq, PartialEq)]
pub enum DataAvailabilityStatus {
    Complete,

    Incomplete,

    None,

    Pending,

    /// Any other value not defined in `DataAvailabilityStatus`.
    UnknownValue(String),
}

#[derive(Clone, Debug, Eq, PartialEq)]
pub enum DataCollectionMode {
    Disabled,

    Enabled,

    /// Any other value not defined in `DataCollectionMode`.
    UnknownValue(String),
}

/// Enum to determine the DataReference credentials type.
#[derive(Clone, Debug, Eq, PartialEq)]
pub enum DataReferenceCredentialType {
    DockerCredentials,

    ManagedIdentity,

    NoCredentials,

    Sas,

    /// Any other value not defined in `DataReferenceCredentialType`.
    UnknownValue(String),
}

/// Enum to determine the type of data.
#[derive(Clone, Debug, Eq, PartialEq)]
pub enum DataType {
    Mltable,

    UriFile,

    UriFolder,

    /// Any other value not defined in `DataType`.
    UnknownValue(String),
}

/// Enum to determine the datastore contents type.
#[derive(Clone, Debug, Eq, PartialEq)]
pub enum DatastoreType {
    AzureBlob,

    AzureDataLakeGen1,

    AzureDataLakeGen2,

    AzureFile,

    OneLake,

    /// Any other value not defined in `DatastoreType`.
    UnknownValue(String),
}

/// Possible values for DeploymentProvisioningState.
#[derive(Clone, Debug, Eq, PartialEq)]
pub enum DeploymentProvisioningState {
    Canceled,

    Creating,

    Deleting,

    Failed,

    Scaling,

    Succeeded,

    Updating,

    /// Any other value not defined in `DeploymentProvisioningState`.
    UnknownValue(String),
}

/// Level of workspace setup error
#[derive(Clone, Debug, Eq, PartialEq)]
pub enum DiagnoseResultLevel {
    Error,

    Information,

    Warning,

    /// Any other value not defined in `DiagnoseResultLevel`.
    UnknownValue(String),
}

/// Enum to determine the job distribution type.
#[derive(Clone, Debug, Eq, PartialEq)]
pub enum DistributionType {
    Mpi,

    PyTorch,

    TensorFlow,

    /// Any other value not defined in `DistributionType`.
    UnknownValue(String),
}

#[derive(Clone, Debug, Eq, PartialEq)]
pub enum EarlyTerminationPolicyType {
    Bandit,

    MedianStopping,

    TruncationSelection,

    /// Any other value not defined in `EarlyTerminationPolicyType`.
    UnknownValue(String),
}

/// Enum to determine whether PublicNetworkAccess is Enabled or Disabled for egress of a deployment.
#[derive(Clone, Debug, Eq, PartialEq)]
pub enum EgressPublicNetworkAccessType {
    Disabled,

    Enabled,

    /// Any other value not defined in `EgressPublicNetworkAccessType`.
    UnknownValue(String),
}

/// Enum to determine the email notification type.
#[derive(Clone, Debug, Eq, PartialEq)]
pub enum EmailNotificationEnableType {
    JobCancelled,

    JobCompleted,

    JobFailed,

    /// Any other value not defined in `EmailNotificationEnableType`.
    UnknownValue(String),
}

/// Indicates whether or not the encryption is enabled for the workspace.
#[derive(Clone, Debug, Eq, PartialEq)]
pub enum EncryptionStatus {
    Disabled,

    Enabled,

    /// Any other value not defined in `EncryptionStatus`.
    UnknownValue(String),
}

/// Enum to determine endpoint authentication mode.
#[derive(Clone, Debug, Eq, PartialEq)]
pub enum EndpointAuthMode {
    AadToken,

    AmlToken,

    Key,

    /// Any other value not defined in `EndpointAuthMode`.
    UnknownValue(String),
}

/// Enum to determine endpoint compute type.
#[derive(Clone, Debug, Eq, PartialEq)]
pub enum EndpointComputeType {
    AzureMlCompute,

    Kubernetes,

    Managed,

    /// Any other value not defined in `EndpointComputeType`.
    UnknownValue(String),
}

/// State of endpoint provisioning.
#[derive(Clone, Debug, Eq, PartialEq)]
pub enum EndpointProvisioningState {
    Canceled,

    Creating,

    Deleting,

    Failed,

    Succeeded,

    Updating,

    /// Any other value not defined in `EndpointProvisioningState`.
    UnknownValue(String),
}

/// Connection status of the service consumer with the service provider
#[derive(Clone, Debug, Eq, PartialEq)]
pub enum EndpointServiceConnectionStatus {
    Approved,

    Disconnected,

    Pending,

    Rejected,

    Timeout,

    /// Any other value not defined in `EndpointServiceConnectionStatus`.
    UnknownValue(String),
}

/// Environment type is either user created or curated by Azure ML service
#[derive(Clone, Debug, Eq, PartialEq)]
pub enum EnvironmentType {
    Curated,

    UserCreated,

    /// Any other value not defined in `EnvironmentType`.
    UnknownValue(String),
}

/// Type of the Environment Variable. Possible values are: local - For local variable
#[derive(Clone, Debug, Eq, PartialEq)]
pub enum EnvironmentVariableType {
    Local,

    /// Any other value not defined in `EnvironmentVariableType`.
    UnknownValue(String),
}

#[derive(Clone, Debug, Eq, PartialEq)]
pub enum FeatureAttributionMetric {
    /// The Normalized Discounted Cumulative Gain metric.
    NormalizedDiscountedCumulativeGain,

    /// Any other value not defined in `FeatureAttributionMetric`.
    UnknownValue(String),
}

#[derive(Clone, Debug, Eq, PartialEq)]
pub enum FeatureDataType {
    Binary,

    Boolean,

    Datetime,

    Double,

    Float,

    Integer,

    Long,

    String,

    /// Any other value not defined in `FeatureDataType`.
    UnknownValue(String),
}

/// The mode of operation for computing feature importance.
#[derive(Clone, Debug, Eq, PartialEq)]
pub enum FeatureImportanceMode {
    /// Disables computing feature importance within a signal.
    Disabled,

    /// Enables computing feature importance within a signal.
    Enabled,

    /// Any other value not defined in `FeatureImportanceMode`.
    UnknownValue(String),
}

/// Flag for generating lags for the numeric features.
#[derive(Clone, Debug, Eq, PartialEq)]
pub enum FeatureLags {
    /// System auto-generates feature lags.
    Auto,

    /// No feature lags generated.
    None,

    /// Any other value not defined in `FeatureLags`.
    UnknownValue(String),
}

/// Featurization mode - determines data featurization mode.
#[derive(Clone, Debug, Eq, PartialEq)]
pub enum FeaturizationMode {
    /// Auto mode, system performs featurization without any custom featurization inputs.
    Auto,

    /// Custom featurization.
    Custom,

    /// Featurization off. 'Forecasting' task cannot use this value.
    Off,

    /// Any other value not defined in `FeaturizationMode`.
    UnknownValue(String),
}

/// Firewall Sku used for FQDN Rules
#[derive(Clone, Debug, Eq, PartialEq)]
pub enum FirewallSku {
    Basic,

    Standard,

    /// Any other value not defined in `FirewallSku`.
    UnknownValue(String),
}

/// Enum to determine forecast horizon selection mode.
#[derive(Clone, Debug, Eq, PartialEq)]
pub enum ForecastHorizonMode {
    /// Forecast horizon to be determined automatically.
    Auto,

    /// Use the custom forecast horizon.
    Custom,

    /// Any other value not defined in `ForecastHorizonMode`.
    UnknownValue(String),
}

/// Enum for all forecasting models supported by AutoML.
#[derive(Clone, Debug, Eq, PartialEq)]
pub enum ForecastingModels {
    /// An Autoregressive Integrated Moving Average with Explanatory Variable (ARIMAX) model can be viewed as a multiple regression
    /// model with one or more autoregressive (AR) terms and/or one or more moving average (MA) terms.
    /// This method is suitable for forecasting when data is stationary/non stationary, and multivariate with any type of data
    /// pattern, i.e., level/trend /seasonality/cyclicity.
    Arimax,

    /// Auto-Autoregressive Integrated Moving Average (ARIMA) model uses time-series data and statistical analysis to interpret
    /// the data and make future predictions.
    /// This model aims to explain data by using time series data on its past values and uses linear regression to make predictions.
    AutoArima,

    /// The Average forecasting model makes predictions by carrying forward the average of the target values for each time-series
    /// in the training data.
    Average,

    /// Decision Trees are a non-parametric supervised learning method used for both classification and regression tasks.
    /// The goal is to create a model that predicts the value of a target variable by learning simple decision rules inferred
    /// from the data features.
    DecisionTree,

    /// Elastic net is a popular type of regularized linear regression that combines two popular penalties, specifically the L1
    /// and L2 penalty functions.
    ElasticNet,

    /// Exponential smoothing is a time series forecasting method for univariate data that can be extended to support data with
    /// a systematic trend or seasonal component.
    ExponentialSmoothing,

    /// Extreme Trees is an ensemble machine learning algorithm that combines the predictions from many decision trees. It is
    /// related to the widely used random forest algorithm.
    ExtremeRandomTrees,

    /// The technique of transiting week learners into a strong learner is called Boosting. The gradient boosting algorithm process
    /// works on this theory of execution.
    GradientBoosting,

    /// K-nearest neighbors (KNN) algorithm uses 'feature similarity' to predict the values of new datapoints
    /// which further means that the new data point will be assigned a value based on how closely it matches the points in the
    /// training set.
    Knn,

    /// Lasso model fit with Least Angle Regression a.k.a. Lars. It is a Linear Model trained with an L1 prior as regularizer.
    LassoLars,

    /// LightGBM is a gradient boosting framework that uses tree based learning algorithms.
    LightGbm,

    /// The Naive forecasting model makes predictions by carrying forward the latest target value for each time-series in the
    /// training data.
    Naive,

    /// Prophet is a procedure for forecasting time series data based on an additive model where non-linear trends are fit with
    /// yearly, weekly, and daily seasonality, plus holiday effects.
    /// It works best with time series that have strong seasonal effects and several seasons of historical data. Prophet is robust
    /// to missing data and shifts in the trend, and typically handles outliers well.
    Prophet,

    /// Random forest is a supervised learning algorithm.
    /// The "forest" it builds, is an ensemble of decision trees, usually trained with the "bagging" method.
    /// The general idea of the bagging method is that a combination of learning models increases the overall result.
    RandomForest,

    /// The Seasonal Average forecasting model makes predictions by carrying forward the average value of the latest season of
    /// data for each time-series in the training data.
    SeasonalAverage,

    /// The Seasonal Naive forecasting model makes predictions by carrying forward the latest season of target values for each
    /// time-series in the training data.
    SeasonalNaive,

    /// SGD: Stochastic gradient descent is an optimization algorithm often used in machine learning applications
    /// to find the model parameters that correspond to the best fit between predicted and actual outputs.
    /// It's an inexact but powerful technique.
    Sgd,

    /// TCNForecaster: Temporal Convolutional Networks Forecaster. //TODO: Ask forecasting team for brief intro.
    TcnForecaster,

    /// XGBoostRegressor: Extreme Gradient Boosting Regressor is a supervised machine learning model using ensemble of base learners.
    XgBoostRegressor,

    /// Any other value not defined in `ForecastingModels`.
    UnknownValue(String),
}

/// Primary metrics for Forecasting task.
#[derive(Clone, Debug, Eq, PartialEq)]
pub enum ForecastingPrimaryMetrics {
    /// The Normalized Mean Absolute Error (NMAE) is a validation metric to compare the Mean Absolute Error (MAE) of (time) series
    /// with different scales.
    NormalizedMeanAbsoluteError,

    /// The Normalized Root Mean Squared Error (NRMSE) the RMSE facilitates the comparison between models with different scales.
    NormalizedRootMeanSquaredError,

    /// The R2 score is one of the performance evaluation measures for forecasting-based machine learning models.
    R2Score,

    /// The Spearman's rank coefficient of correlation is a non-parametric measure of rank correlation.
    SpearmanCorrelation,

    /// Any other value not defined in `ForecastingPrimaryMetrics`.
    UnknownValue(String),
}

/// Defines supported metric goals for hyperparameter tuning
#[derive(Clone, Debug, Eq, PartialEq)]
pub enum Goal {
    Maximize,

    Minimize,

    /// Any other value not defined in `Goal`.
    UnknownValue(String),
}

/// Enum to determine identity framework.
#[derive(Clone, Debug, Eq, PartialEq)]
pub enum IdentityConfigurationType {
    AmlToken,

    Managed,

    UserIdentity,

    /// Any other value not defined in `IdentityConfigurationType`.
    UnknownValue(String),
}

/// Type of the image. Possible values are: docker - For docker images. azureml - For AzureML Environment images (custom and
/// curated)
#[derive(Clone, Debug, Eq, PartialEq)]
pub enum ImageType {
    Azureml,

    Docker,

    /// Any other value not defined in `ImageType`.
    UnknownValue(String),
}

/// Enum to determine the input data delivery mode.
#[derive(Clone, Debug, Eq, PartialEq)]
pub enum InputDeliveryMode {
    Direct,

    Download,

    EvalDownload,

    EvalMount,

    ReadOnlyMount,

    ReadWriteMount,

    /// Any other value not defined in `InputDeliveryMode`.
    UnknownValue(String),
}

/// Primary metrics for InstanceSegmentation tasks.
#[derive(Clone, Debug, Eq, PartialEq)]
pub enum InstanceSegmentationPrimaryMetrics {
    /// Mean Average Precision (MAP) is the average of AP (Average Precision).
    /// AP is calculated for each class and averaged to get the MAP.
    MeanAveragePrecision,

    /// Any other value not defined in `InstanceSegmentationPrimaryMetrics`.
    UnknownValue(String),
}

/// Isolation mode for the managed network of a machine learning workspace.
#[derive(Clone, Debug, Eq, PartialEq)]
pub enum IsolationMode {
    AllowInternetOutbound,

    AllowOnlyApprovedOutbound,

    Disabled,

    /// Any other value not defined in `IsolationMode`.
    UnknownValue(String),
}

/// Enum to determine the Job Input Type.
#[derive(Clone, Debug, Eq, PartialEq)]
pub enum JobInputType {
    CustomModel,

    Literal,

    MlflowModel,

    Mltable,

    TritonModel,

    UriFile,

    UriFolder,

    /// Any other value not defined in `JobInputType`.
    UnknownValue(String),
}

#[derive(Clone, Debug, Eq, PartialEq)]
pub enum JobLimitsType {
    Command,

    Sweep,

    /// Any other value not defined in `JobLimitsType`.
    UnknownValue(String),
}

/// Enum to determine the Job Output Type.
#[derive(Clone, Debug, Eq, PartialEq)]
pub enum JobOutputType {
    CustomModel,

    MlflowModel,

    Mltable,

    TritonModel,

    UriFile,

    UriFolder,

    /// Any other value not defined in `JobOutputType`.
    UnknownValue(String),
}

/// The status of a job.
#[derive(Clone, Debug, Eq, PartialEq)]
pub enum JobStatus {
    /// Cancellation has been requested for the job.
    CancelRequested,

    /// Following cancellation request, the job is now successfully canceled.
    Canceled,

    /// Job completed successfully. This reflects that both the job itself and output collection states completed successfully
    Completed,

    /// Job failed.
    Failed,

    /// Job is completed in the target. It is in output collection state now.
    Finalizing,

    /// When heartbeat is enabled, if the run isn't updating any information to RunHistory then the run goes to NotResponding
    /// state.
    /// NotResponding is the only state that is exempt from strict transition orders. A run can go from NotResponding to any of
    /// the previous states.
    NotResponding,

    /// Run hasn't started yet.
    NotStarted,

    /// The job is paused by users. Some adjustment to labeling jobs can be made only in paused state.
    Paused,

    /// The run environment is being prepared.
    Preparing,

    /// (Not used currently) It will be used if ES is creating the compute target.
    Provisioning,

    /// The job is queued in the compute target. For example, in BatchAI the job is in queued state, while waiting for all required
    /// nodes to be ready.
    Queued,

    /// The job started to run in the compute target.
    Running,

    /// Run has started. The user has a run ID.
    Starting,

    /// Default job status if not mapped to all other statuses
    Unknown,

    /// Any other value not defined in `JobStatus`.
    UnknownValue(String),
}

/// Enum to determine the job tier.
#[derive(Clone, Debug, Eq, PartialEq)]
pub enum JobTier {
    Basic,

    Null,

    Premium,

    Spot,

    Standard,

    /// Any other value not defined in `JobTier`.
    UnknownValue(String),
}

/// Enum to determine the type of job.
#[derive(Clone, Debug, Eq, PartialEq)]
pub enum JobType {
    AutoMl,

    Command,

    Pipeline,

    Spark,

    Sweep,

    /// Any other value not defined in `JobType`.
    UnknownValue(String),
}

#[derive(Clone, Debug, Eq, PartialEq)]
pub enum KeyType {
    Primary,

    Secondary,

    /// Any other value not defined in `KeyType`.
    UnknownValue(String),
}

/// Learning rate scheduler enum.
#[derive(Clone, Debug, Eq, PartialEq)]
pub enum LearningRateScheduler {
    /// No learning rate scheduler selected.
    None,

    /// Step learning rate scheduler.
    Step,

    /// Cosine Annealing With Warmup.
    WarmupCosine,

    /// Any other value not defined in `LearningRateScheduler`.
    UnknownValue(String),
}

#[derive(Clone, Debug, Eq, PartialEq)]
pub enum ListViewType {
    ActiveOnly,

    All,

    ArchivedOnly,

    /// Any other value not defined in `ListViewType`.
    UnknownValue(String),
}

/// Load Balancer Type
#[derive(Clone, Debug, Eq, PartialEq)]
pub enum LoadBalancerType {
    InternalLoadBalancer,

    PublicIP,

    /// Any other value not defined in `LoadBalancerType`.
    UnknownValue(String),
}

/// Enum for setting log verbosity.
#[derive(Clone, Debug, Eq, PartialEq)]
pub enum LogVerbosity {
    /// Only critical statements logged.
    Critical,

    /// Debug and above log statements logged.
    Debug,

    /// Error and above log statements logged.
    Error,

    /// Info and above log statements logged.
    Info,

    /// No logs emitted.
    NotSet,

    /// Warning and above log statements logged.
    Warning,

    /// Any other value not defined in `LogVerbosity`.
    UnknownValue(String),
}

/// The Kind of the managed network. Users can switch from V1 to V2 for granular access controls, but cannot switch back to
/// V1 once V2 is enabled.
#[derive(Clone, Debug, Eq, PartialEq)]
pub enum ManagedNetworkKind {
    V1,

    V2,

    /// Any other value not defined in `ManagedNetworkKind`.
    UnknownValue(String),
}

/// Status for the managed network of a machine learning workspace.
#[derive(Clone, Debug, Eq, PartialEq)]
pub enum ManagedNetworkStatus {
    Active,

    Inactive,

    /// Any other value not defined in `ManagedNetworkStatus`.
    UnknownValue(String),
}

#[derive(Clone, Debug, Eq, PartialEq)]
pub enum ManagedPeRequirement {
    NotApplicable,

    NotRequired,

    Required,

    /// Any other value not defined in `ManagedPeRequirement`.
    UnknownValue(String),
}

#[derive(Clone, Debug, Eq, PartialEq)]
pub enum ManagedPeStatus {
    Active,

    Inactive,

    NotApplicable,

    /// Any other value not defined in `ManagedPeStatus`.
    UnknownValue(String),
}

/// Type of managed service identity (where both SystemAssigned and UserAssigned types are allowed).
#[derive(Clone, Debug, Eq, PartialEq)]
pub enum ManagedServiceIdentityType {
    /// No managed identity.
    None,

    /// System assigned managed identity.
    SystemAssigned,

    /// System and user assigned managed identity.
    SystemAssignedUserAssigned,

    /// User assigned managed identity.
    UserAssigned,

    /// Any other value not defined in `ManagedServiceIdentityType`.
    UnknownValue(String),
}

#[derive(Clone, Debug, Eq, PartialEq)]
pub enum MarketplaceSubscriptionProvisioningState {
    Canceled,

    /// MarketplaceSubscription is being created.
    Creating,

    /// MarketplaceSubscription is being deleted.
    Deleting,

    /// MarketplaceSubscription provisioning failed.
    Failed,

    /// MarketplaceSubscription is successfully provisioned.
    Succeeded,

    /// MarketplaceSubscription is being updated.
    Updating,

    /// Any other value not defined in `MarketplaceSubscriptionProvisioningState`.
    UnknownValue(String),
}

#[derive(Clone, Debug, Eq, PartialEq)]
pub enum MarketplaceSubscriptionStatus {
    /// The customer can now use the Marketplace Subscription's
    /// model and will be billed.
    Subscribed,

    /// The customer could not be billed for the Marketplace Subscription.
    /// The customer will not be able to access the model.
    Suspended,

    /// Marketplace Subscriptions reach this state in response to an explicit customer or CSP action.
    /// A Marketplace Subscription can also be canceled implicitly, as a result of nonpayment of dues,
    /// after being in the Suspended state for some time.
    Unsubscribed,

    /// Any other value not defined in `MarketplaceSubscriptionStatus`.
    UnknownValue(String),
}

#[derive(Clone, Debug, Eq, PartialEq)]
pub enum MaterializationStoreType {
    None,

    Offline,

    Online,

    OnlineAndOffline,

    /// Any other value not defined in `MaterializationStoreType`.
    UnknownValue(String),
}

/// Image model size.
#[derive(Clone, Debug, Eq, PartialEq)]
pub enum ModelSize {
    /// Extra large size.
    ExtraLarge,

    /// Large size.
    Large,

    /// Medium size.
    Medium,

    /// No value selected.
    None,

    /// Small size.
    Small,

    /// Any other value not defined in `ModelSize`.
    UnknownValue(String),
}

/// Model task type enum.
#[derive(Clone, Debug, Eq, PartialEq)]
pub enum ModelTaskType {
    Classification,

    Regression,

    /// Any other value not defined in `ModelTaskType`.
    UnknownValue(String),
}

/// Monitor compute identity type enum.
#[derive(Clone, Debug, Eq, PartialEq)]
pub enum MonitorComputeIdentityType {
    /// Authenticates through user's AML token.
    AmlToken,

    /// Authenticates through a user-provided managed identity.
    ManagedIdentity,

    /// Any other value not defined in `MonitorComputeIdentityType`.
    UnknownValue(String),
}

/// Monitor compute type enum.
#[derive(Clone, Debug, Eq, PartialEq)]
pub enum MonitorComputeType {
    /// Serverless Spark compute.
    ServerlessSpark,

    /// Any other value not defined in `MonitorComputeType`.
    UnknownValue(String),
}

#[derive(Clone, Debug, Eq, PartialEq)]
pub enum MonitoringFeatureDataType {
    /// Used for features of categorical data type.
    Categorical,

    /// Used for features of numerical data type.
    Numerical,

    /// Any other value not defined in `MonitoringFeatureDataType`.
    UnknownValue(String),
}

#[derive(Clone, Debug, Eq, PartialEq)]
pub enum MonitoringFeatureFilterType {
    /// Includes all features.
    AllFeatures,

    /// Includes a user-defined subset of features.
    FeatureSubset,

    /// Only includes the top contributing features, measured by feature attribution.
    TopNByAttribution,

    /// Any other value not defined in `MonitoringFeatureFilterType`.
    UnknownValue(String),
}

/// Monitoring input data type enum.
#[derive(Clone, Debug, Eq, PartialEq)]
pub enum MonitoringInputDataType {
    /// An input data with tabular format which doesn't require preprocessing.
    Fixed,

    /// An input data which rolls relatively to the monitor's current run time.
    Rolling,

    /// An input data with a fixed window size.
    Static,

    /// Any other value not defined in `MonitoringInputDataType`.
    UnknownValue(String),
}

#[derive(Clone, Debug, Eq, PartialEq)]
pub enum MonitoringNotificationType {
    /// Enables email notifications through AML notifications.
    AmlNotification,

    /// Any other value not defined in `MonitoringNotificationType`.
    UnknownValue(String),
}

#[derive(Clone, Debug, Eq, PartialEq)]
pub enum MonitoringSignalType {
    /// Tracks a custom signal provided by users.
    Custom,

    /// Tracks model input data distribution change, comparing against training data or past production data.
    DataDrift,

    /// Tracks model input data integrity.
    DataQuality,

    /// Tracks feature importance change in production, comparing against feature importance at training time.
    FeatureAttributionDrift,

    /// Tracks prediction result data distribution change, comparing against validation/test label data or past production data.
    PredictionDrift,

    /// Any other value not defined in `MonitoringSignalType`.
    UnknownValue(String),
}

/// Mount Action.
#[derive(Clone, Debug, Eq, PartialEq)]
pub enum MountAction {
    Mount,

    Unmount,

    /// Any other value not defined in `MountAction`.
    UnknownValue(String),
}

/// Mount Mode.
#[derive(Clone, Debug, Eq, PartialEq)]
pub enum MountMode {
    ReadOnly,

    ReadWrite,

    /// Any other value not defined in `MountMode`.
    UnknownValue(String),
}

/// Mount state.
#[derive(Clone, Debug, Eq, PartialEq)]
pub enum MountState {
    MountFailed,

    MountRequested,

    Mounted,

    UnmountFailed,

    UnmountRequested,

    Unmounted,

    /// Any other value not defined in `MountState`.
    UnknownValue(String),
}

/// Determines how N-Cross validations value is determined.
#[derive(Clone, Debug, Eq, PartialEq)]
pub enum NCrossValidationsMode {
    /// Determine N-Cross validations value automatically. Supported only for 'Forecasting' AutoML task.
    Auto,

    /// Use custom N-Cross validations value.
    Custom,

    /// Any other value not defined in `NCrossValidationsMode`.
    UnknownValue(String),
}

/// network of this container.
#[derive(Clone, Debug, Eq, PartialEq)]
pub enum Network {
    Bridge,

    Host,

    /// Any other value not defined in `Network`.
    UnknownValue(String),
}

/// State of the compute node. Values are idle, running, preparing, unusable, leaving and preempted.
#[derive(Clone, Debug, Eq, PartialEq)]
pub enum NodeState {
    Idle,

    Leaving,

    Preempted,

    Preparing,

    Running,

    Unusable,

    /// Any other value not defined in `NodeState`.
    UnknownValue(String),
}

/// The enumerated types for the nodes value
#[derive(Clone, Debug, Eq, PartialEq)]
pub enum NodesValueType {
    All,

    /// Any other value not defined in `NodesValueType`.
    UnknownValue(String),
}

#[derive(Clone, Debug, Eq, PartialEq)]
pub enum NumericalDataDriftMetric {
    /// The Jensen Shannon Distance (JSD) metric.
    JensenShannonDistance,

    /// The Normalized Wasserstein Distance metric.
    NormalizedWassersteinDistance,

    /// The Population Stability Index (PSI) metric.
    PopulationStabilityIndex,

    /// The Two Sample Kolmogorov-Smirnov Test (two-sample Kâ€“S) metric.
    TwoSampleKolmogorovSmirnovTest,

    /// Any other value not defined in `NumericalDataDriftMetric`.
    UnknownValue(String),
}

#[derive(Clone, Debug, Eq, PartialEq)]
pub enum NumericalDataQualityMetric {
    /// Calculates the rate of data type errors.
    DataTypeErrorRate,

    /// Calculates the rate of null values.
    NullValueRate,

    /// Calculates the rate values are out of bounds.
    OutOfBoundsRate,

    /// Any other value not defined in `NumericalDataQualityMetric`.
    UnknownValue(String),
}

#[derive(Clone, Debug, Eq, PartialEq)]
pub enum NumericalPredictionDriftMetric {
    /// The Jensen Shannon Distance (JSD) metric.
    JensenShannonDistance,

    /// The Normalized Wasserstein Distance metric.
    NormalizedWassersteinDistance,

    /// The Population Stability Index (PSI) metric.
    PopulationStabilityIndex,

    /// The Two Sample Kolmogorov-Smirnov Test (two-sample Kâ€“S) metric.
    TwoSampleKolmogorovSmirnovTest,

    /// Any other value not defined in `NumericalPredictionDriftMetric`.
    UnknownValue(String),
}

/// Compute OS Type
#[derive(Clone, Debug, Eq, PartialEq)]
pub enum OSType {
    Linux,

    Windows,

    /// Any other value not defined in `OSType`.
    UnknownValue(String),
}

/// Primary metrics for Image ObjectDetection task.
#[derive(Clone, Debug, Eq, PartialEq)]
pub enum ObjectDetectionPrimaryMetrics {
    /// Mean Average Precision (MAP) is the average of AP (Average Precision).
    /// AP is calculated for each class and averaged to get the MAP.
    MeanAveragePrecision,

    /// Any other value not defined in `ObjectDetectionPrimaryMetrics`.
    UnknownValue(String),
}

/// Enum to determine OneLake artifact type.
#[derive(Clone, Debug, Eq, PartialEq)]
pub enum OneLakeArtifactType {
    LakeHouse,

    /// Any other value not defined in `OneLakeArtifactType`.
    UnknownValue(String),
}

/// The type of operating system.
#[derive(Clone, Debug, Eq, PartialEq)]
pub enum OperatingSystemType {
    Linux,

    Windows,

    /// Any other value not defined in `OperatingSystemType`.
    UnknownValue(String),
}

/// Name of the last operation.
#[derive(Clone, Debug, Eq, PartialEq)]
pub enum OperationName {
    Create,

    Delete,

    Reimage,

    Resize,

    Restart,

    Start,

    Stop,

    /// Any other value not defined in `OperationName`.
    UnknownValue(String),
}

/// Operation status.
#[derive(Clone, Debug, Eq, PartialEq)]
pub enum OperationStatus {
    CreateFailed,

    DeleteFailed,

    InProgress,

    ReimageFailed,

    ResizeFailed,

    RestartFailed,

    StartFailed,

    StopFailed,

    Succeeded,

    /// Any other value not defined in `OperationStatus`.
    UnknownValue(String),
}

/// Trigger of operation.
#[derive(Clone, Debug, Eq, PartialEq)]
pub enum OperationTrigger {
    IdleShutdown,

    Schedule,

    User,

    /// Any other value not defined in `OperationTrigger`.
    UnknownValue(String),
}

#[derive(Clone, Debug, Eq, PartialEq)]
pub enum OrderString {
    CreatedAtAsc,

    CreatedAtDesc,

    UpdatedAtAsc,

    UpdatedAtDesc,

    /// Any other value not defined in `OrderString`.
    UnknownValue(String),
}

/// The intended executor of the operation; as in Resource Based Access Control (RBAC) and audit logs UX. Default value is
/// "user,system"
#[derive(Clone, Debug, Eq, PartialEq)]
pub enum Origin {
    /// Indicates the operation is initiated by a system.
    System,

    /// Indicates the operation is initiated by a user.
    User,

    /// Indicates the operation is initiated by a user or system.
    UserSystem,

    /// Any other value not defined in `Origin`.
    UnknownValue(String),
}

/// Output data delivery mode enums.
#[derive(Clone, Debug, Eq, PartialEq)]
pub enum OutputDeliveryMode {
    Direct,

    ReadWriteMount,

    Upload,

    /// Any other value not defined in `OutputDeliveryMode`.
    UnknownValue(String),
}

/// The os patching status.
#[derive(Clone, Debug, Eq, PartialEq)]
pub enum PatchStatus {
    CompletedWithWarnings,

    Failed,

    InProgress,

    Succeeded,

    Unknown,

    /// Any other value not defined in `PatchStatus`.
    UnknownValue(String),
}

/// Enum to determine the PendingUpload credentials type.
#[derive(Clone, Debug, Eq, PartialEq)]
pub enum PendingUploadCredentialType {
    Sas,

    /// Any other value not defined in `PendingUploadCredentialType`.
    UnknownValue(String),
}

/// Type of storage to use for the pending upload location
#[derive(Clone, Debug, Eq, PartialEq)]
pub enum PendingUploadType {
    None,

    TemporaryBlobReference,

    /// Any other value not defined in `PendingUploadType`.
    UnknownValue(String),
}

/// The current provisioning state.
#[derive(Clone, Debug, Eq, PartialEq)]
pub enum PrivateEndpointConnectionProvisioningState {
    Creating,

    Deleting,

    Failed,

    Succeeded,

    /// Any other value not defined in `PrivateEndpointConnectionProvisioningState`.
    UnknownValue(String),
}

/// Protocol over which communication will happen over this endpoint
#[derive(Clone, Debug, Eq, PartialEq)]
pub enum Protocol {
    Http,

    Tcp,

    Udp,

    /// Any other value not defined in `Protocol`.
    UnknownValue(String),
}

/// The provision state of the cluster. Valid values are Unknown, Updating, Provisioning, Succeeded, and Failed.
#[derive(Clone, Debug, Eq, PartialEq)]
pub enum ProvisioningState {
    Canceled,

    Creating,

    Deleting,

    Failed,

    Succeeded,

    Unknown,

    Updating,

    /// Any other value not defined in `ProvisioningState`.
    UnknownValue(String),
}

/// The current deployment state of schedule.
#[derive(Clone, Debug, Eq, PartialEq)]
pub enum ProvisioningStatus {
    Completed,

    Failed,

    Provisioning,

    /// Any other value not defined in `ProvisioningStatus`.
    UnknownValue(String),
}

/// Enum to determine whether PublicNetworkAccess is Enabled or Disabled.
#[derive(Clone, Debug, Eq, PartialEq)]
pub enum PublicNetworkAccessType {
    Disabled,

    Enabled,

    /// Any other value not defined in `PublicNetworkAccessType`.
    UnknownValue(String),
}

/// An enum describing the unit of quota measurement.
#[derive(Clone, Debug, Eq, PartialEq)]
pub enum QuotaUnit {
    Count,

    /// Any other value not defined in `QuotaUnit`.
    UnknownValue(String),
}

/// The specific type of random algorithm
#[derive(Clone, Debug, Eq, PartialEq)]
pub enum RandomSamplingAlgorithmRule {
    Random,

    Sobol,

    /// Any other value not defined in `RandomSamplingAlgorithmRule`.
    UnknownValue(String),
}

/// Enum to describe the frequency of a recurrence schedule
#[derive(Clone, Debug, Eq, PartialEq)]
pub enum RecurrenceFrequency {
    /// Day frequency
    Day,

    /// Hour frequency
    Hour,

    /// Minute frequency
    Minute,

    /// Month frequency
    Month,

    /// Week frequency
    Week,

    /// Any other value not defined in `RecurrenceFrequency`.
    UnknownValue(String),
}

/// Enum to determine which reference method to use for an asset.
#[derive(Clone, Debug, Eq, PartialEq)]
pub enum ReferenceType {
    DataPath,

    Id,

    OutputPath,

    /// Any other value not defined in `ReferenceType`.
    UnknownValue(String),
}

/// Enum for all Regression models supported by AutoML.
#[derive(Clone, Debug, Eq, PartialEq)]
pub enum RegressionModels {
    /// Decision Trees are a non-parametric supervised learning method used for both classification and regression tasks.
    /// The goal is to create a model that predicts the value of a target variable by learning simple decision rules inferred
    /// from the data features.
    DecisionTree,

    /// Elastic net is a popular type of regularized linear regression that combines two popular penalties, specifically the L1
    /// and L2 penalty functions.
    ElasticNet,

    /// Extreme Trees is an ensemble machine learning algorithm that combines the predictions from many decision trees. It is
    /// related to the widely used random forest algorithm.
    ExtremeRandomTrees,

    /// The technique of transiting week learners into a strong learner is called Boosting. The gradient boosting algorithm process
    /// works on this theory of execution.
    GradientBoosting,

    /// K-nearest neighbors (KNN) algorithm uses 'feature similarity' to predict the values of new datapoints
    /// which further means that the new data point will be assigned a value based on how closely it matches the points in the
    /// training set.
    Knn,

    /// Lasso model fit with Least Angle Regression a.k.a. Lars. It is a Linear Model trained with an L1 prior as regularizer.
    LassoLars,

    /// LightGBM is a gradient boosting framework that uses tree based learning algorithms.
    LightGbm,

    /// Random forest is a supervised learning algorithm.
    /// The "forest" it builds, is an ensemble of decision trees, usually trained with the "bagging" method.
    /// The general idea of the bagging method is that a combination of learning models increases the overall result.
    RandomForest,

    /// SGD: Stochastic gradient descent is an optimization algorithm often used in machine learning applications
    /// to find the model parameters that correspond to the best fit between predicted and actual outputs.
    /// It's an inexact but powerful technique.
    Sgd,

    /// XGBoostRegressor: Extreme Gradient Boosting Regressor is a supervised machine learning model using ensemble of base learners.
    XgBoostRegressor,

    /// Any other value not defined in `RegressionModels`.
    UnknownValue(String),
}

/// Primary metrics for Regression task.
#[derive(Clone, Debug, Eq, PartialEq)]
pub enum RegressionPrimaryMetrics {
    /// The Normalized Mean Absolute Error (NMAE) is a validation metric to compare the Mean Absolute Error (MAE) of (time) series
    /// with different scales.
    NormalizedMeanAbsoluteError,

    /// The Normalized Root Mean Squared Error (NRMSE) the RMSE facilitates the comparison between models with different scales.
    NormalizedRootMeanSquaredError,

    /// The R2 score is one of the performance evaluation measures for forecasting-based machine learning models.
    R2Score,

    /// The Spearman's rank coefficient of correlation is a nonparametric measure of rank correlation.
    SpearmanCorrelation,

    /// Any other value not defined in `RegressionPrimaryMetrics`.
    UnknownValue(String),
}

/// State of the public SSH port. Possible values are: Disabled - Indicates that the public ssh port is closed on all nodes
/// of the cluster. Enabled - Indicates that the public ssh port is open on all nodes of the cluster. NotSpecified - Indicates
/// that the public ssh port is closed on all nodes of the cluster if VNet is defined, else is open all public nodes. It can
/// be default only during cluster creation time, after creation it will be either enabled or disabled.
#[derive(Clone, Debug, Eq, PartialEq)]
pub enum RemoteLoginPortPublicAccess {
    Disabled,

    Enabled,

    NotSpecified,

    /// Any other value not defined in `RemoteLoginPortPublicAccess`.
    UnknownValue(String),
}

/// The provisioning state of a resource type.
#[derive(Clone, Debug, Eq, PartialEq)]
pub enum ResourceProvisioningState {
    /// Resource creation was canceled.
    Canceled,

    /// Resource creation failed.
    Failed,

    /// Resource has been created.
    Succeeded,

    /// Any other value not defined in `ResourceProvisioningState`.
    UnknownValue(String),
}

#[derive(Clone, Debug, Eq, PartialEq)]
pub enum RollingRateType {
    Day,

    Hour,

    Minute,

    Month,

    Year,

    /// Any other value not defined in `RollingRateType`.
    UnknownValue(String),
}

/// The action enum for networking rule.
#[derive(Clone, Debug, Eq, PartialEq)]
pub enum RuleAction {
    Allow,

    Deny,

    /// Any other value not defined in `RuleAction`.
    UnknownValue(String),
}

/// Category of a managed network Outbound Rule of a machine learning workspace.
#[derive(Clone, Debug, Eq, PartialEq)]
pub enum RuleCategory {
    Dependency,

    Recommended,

    Required,

    UserDefined,

    /// Any other value not defined in `RuleCategory`.
    UnknownValue(String),
}

/// Type of a managed network Outbound Rule of a machine learning workspace.
#[derive(Clone, Debug, Eq, PartialEq)]
pub enum RuleStatus {
    Active,

    Deleting,

    Failed,

    Inactive,

    Provisioning,

    /// Any other value not defined in `RuleStatus`.
    UnknownValue(String),
}

/// Type of a managed network Outbound Rule of a machine learning workspace.
#[derive(Clone, Debug, Eq, PartialEq)]
pub enum RuleType {
    Fqdn,

    PrivateEndpoint,

    ServiceTag,

    /// Any other value not defined in `RuleType`.
    UnknownValue(String),
}

#[derive(Clone, Debug, Eq, PartialEq)]
pub enum SamplingAlgorithmType {
    Bayesian,

    Grid,

    Random,

    /// Any other value not defined in `SamplingAlgorithmType`.
    UnknownValue(String),
}

#[derive(Clone, Debug, Eq, PartialEq)]
pub enum ScaleType {
    Default,

    TargetUtilization,

    /// Any other value not defined in `ScaleType`.
    UnknownValue(String),
}

#[derive(Clone, Debug, Eq, PartialEq)]
pub enum ScheduleActionType {
    CreateJob,

    CreateMonitor,

    InvokeBatchEndpoint,

    /// Any other value not defined in `ScheduleActionType`.
    UnknownValue(String),
}

#[derive(Clone, Debug, Eq, PartialEq)]
pub enum ScheduleListViewType {
    All,

    DisabledOnly,

    EnabledOnly,

    /// Any other value not defined in `ScheduleListViewType`.
    UnknownValue(String),
}

/// The current deployment state of schedule.
#[derive(Clone, Debug, Eq, PartialEq)]
pub enum ScheduleProvisioningState {
    Completed,

    Failed,

    Provisioning,

    /// Any other value not defined in `ScheduleProvisioningState`.
    UnknownValue(String),
}

#[derive(Clone, Debug, Eq, PartialEq)]
pub enum ScheduleProvisioningStatus {
    Canceled,

    Creating,

    Deleting,

    Failed,

    Succeeded,

    Updating,

    /// Any other value not defined in `ScheduleProvisioningStatus`.
    UnknownValue(String),
}

/// Is the schedule enabled or disabled?
#[derive(Clone, Debug, Eq, PartialEq)]
pub enum ScheduleStatus {
    Disabled,

    Enabled,

    /// Any other value not defined in `ScheduleStatus`.
    UnknownValue(String),
}

/// Forecasting seasonality mode.
#[derive(Clone, Debug, Eq, PartialEq)]
pub enum SeasonalityMode {
    /// Seasonality to be determined automatically.
    Auto,

    /// Use the custom seasonality value.
    Custom,

    /// Any other value not defined in `SeasonalityMode`.
    UnknownValue(String),
}

/// Enum to determine the datastore secrets type.
#[derive(Clone, Debug, Eq, PartialEq)]
pub enum SecretsType {
    AccountKey,

    Certificate,

    Sas,

    ServicePrincipal,

    /// Any other value not defined in `SecretsType`.
    UnknownValue(String),
}

/// State of the Serverless Endpoint.
#[derive(Clone, Debug, Eq, PartialEq)]
pub enum ServerlessEndpointState {
    Creating,

    CreationFailed,

    Deleting,

    DeletionFailed,

    Online,

    Reinstating,

    Suspended,

    Suspending,

    Unknown,

    /// Any other value not defined in `ServerlessEndpointState`.
    UnknownValue(String),
}

#[derive(Clone, Debug, Eq, PartialEq)]
pub enum ServerlessInferenceEndpointAuthMode {
    Aad,

    Key,

    KeyAndAad,

    /// Any other value not defined in `ServerlessInferenceEndpointAuthMode`.
    UnknownValue(String),
}

#[derive(Clone, Debug, Eq, PartialEq)]
pub enum ServiceDataAccessAuthIdentity {
    /// Do not use any identity for service data access.
    None,

    /// Use the system assigned managed identity of the Workspace to authenticate service data access.
    WorkspaceSystemAssignedIdentity,

    /// Use the user assigned managed identity of the Workspace to authenticate service data access.
    WorkspaceUserAssignedIdentity,

    /// Any other value not defined in `ServiceDataAccessAuthIdentity`.
    UnknownValue(String),
}

/// The parameter defining how if AutoML should handle short time series.
#[derive(Clone, Debug, Eq, PartialEq)]
pub enum ShortSeriesHandlingConfiguration {
    /// Short series will be padded if there are no long series, otherwise short series will be dropped.
    Auto,

    /// All the short series will be dropped.
    Drop,

    /// Represents no/null value.
    None,

    /// All the short series will be padded.
    Pad,

    /// Any other value not defined in `ShortSeriesHandlingConfiguration`.
    UnknownValue(String),
}

/// Node scaling setting for the compute sku.
#[derive(Clone, Debug, Eq, PartialEq)]
pub enum SkuScaleType {
    /// Automatically scales node count.
    Automatic,

    /// Node count scaled upon user request.
    Manual,

    /// Fixed set of nodes.
    None,

    /// Any other value not defined in `SkuScaleType`.
    UnknownValue(String),
}

/// This field is required to be implemented by the Resource Provider if the service has more than one tier, but is not required
/// on a PUT.
#[derive(Clone, Copy, Debug, Eq, PartialEq)]
pub enum SkuTier {
    /// The Basic service tier.
    Basic,

    /// The Free service tier.
    Free,

    /// The Premium service tier.
    Premium,

    /// The Standard service tier.
    Standard,
}

/// Data source type.
#[derive(Clone, Debug, Eq, PartialEq)]
pub enum SourceType {
    Dataset,

    Datastore,

    Uri,

    /// Any other value not defined in `SourceType`.
    UnknownValue(String),
}

#[derive(Clone, Debug, Eq, PartialEq)]
pub enum SparkJobEntryType {
    SparkJobPythonEntry,

    SparkJobScalaEntry,

    /// Any other value not defined in `SparkJobEntryType`.
    UnknownValue(String),
}

/// State of the public SSH port. Possible values are: Disabled - Indicates that the public ssh port is closed on this instance.
/// Enabled - Indicates that the public ssh port is open and accessible according to the VNet/subnet policy if applicable.
#[derive(Clone, Debug, Eq, PartialEq)]
pub enum SshPublicAccess {
    Disabled,

    Enabled,

    /// Any other value not defined in `SshPublicAccess`.
    UnknownValue(String),
}

/// Enable or disable ssl for scoring
#[derive(Clone, Debug, Eq, PartialEq)]
pub enum SslConfigStatus {
    Auto,

    Disabled,

    Enabled,

    /// Any other value not defined in `SslConfigStatus`.
    UnknownValue(String),
}

/// The meta-learner is a model trained on the output of the individual heterogeneous models.
/// Default meta-learners are LogisticRegression for classification tasks (or LogisticRegressionCV if cross-validation is
/// enabled) and ElasticNet for regression/forecasting tasks (or ElasticNetCV if cross-validation is enabled).
/// This parameter can be one of the following strings: LogisticRegression, LogisticRegressionCV, LightGBMClassifier, ElasticNet,
/// ElasticNetCV, LightGBMRegressor, or LinearRegression
#[derive(Clone, Debug, Eq, PartialEq)]
pub enum StackMetaLearnerType {
    /// Default meta-learners are LogisticRegression for regression task.
    ElasticNet,

    /// Default meta-learners are LogisticRegression for regression task when CV is on.
    ElasticNetCv,

    LightGbmClassifier,

    LightGbmRegressor,

    LinearRegression,

    /// Default meta-learners are LogisticRegression for classification tasks.
    LogisticRegression,

    /// Default meta-learners are LogisticRegression for classification task when CV is on.
    LogisticRegressionCv,

    None,

    /// Any other value not defined in `StackMetaLearnerType`.
    UnknownValue(String),
}

/// Status of update workspace quota.
#[derive(Clone, Debug, Eq, PartialEq)]
pub enum Status {
    Failure,

    InvalidQuotaBelowClusterMinimum,

    InvalidQuotaExceedsSubscriptionLimit,

    InvalidVMFamilyName,

    OperationNotEnabledForRegion,

    OperationNotSupportedForSku,

    Success,

    Undefined,

    /// Any other value not defined in `Status`.
    UnknownValue(String),
}

/// Stochastic optimizer for image models.
#[derive(Clone, Debug, Eq, PartialEq)]
pub enum StochasticOptimizer {
    /// Adam is algorithm the optimizes stochastic objective functions based on adaptive estimates of moments
    Adam,

    /// AdamW is a variant of the optimizer Adam that has an improved implementation of weight decay.
    Adamw,

    /// No optimizer selected.
    None,

    /// Stochastic Gradient Descent optimizer.
    Sgd,

    /// Any other value not defined in `StochasticOptimizer`.
    UnknownValue(String),
}

/// type of this storage account.
#[derive(Clone, Debug, Eq, PartialEq)]
pub enum StorageAccountType {
    PremiumLrs,

    StandardLrs,

    /// Any other value not defined in `StorageAccountType`.
    UnknownValue(String),
}

/// The auth mode used for accessing the system datastores of the workspace.
#[derive(Clone, Debug, Eq, PartialEq)]
pub enum SystemDatastoresAuthMode {
    AccessKey,

    Identity,

    UserDelegationSas,

    /// Any other value not defined in `SystemDatastoresAuthMode`.
    UnknownValue(String),
}

/// Target aggregate function.
#[derive(Clone, Debug, Eq, PartialEq)]
pub enum TargetAggregationFunction {
    Max,

    Mean,

    Min,

    /// Represent no value set.
    None,

    Sum,

    /// Any other value not defined in `TargetAggregationFunction`.
    UnknownValue(String),
}

/// Target lags selection modes.
#[derive(Clone, Debug, Eq, PartialEq)]
pub enum TargetLagsMode {
    /// Target lags to be determined automatically.
    Auto,

    /// Use the custom target lags.
    Custom,

    /// Any other value not defined in `TargetLagsMode`.
    UnknownValue(String),
}

/// Target rolling windows size mode.
#[derive(Clone, Debug, Eq, PartialEq)]
pub enum TargetRollingWindowSizeMode {
    /// Determine rolling windows size automatically.
    Auto,

    /// Use the specified rolling window size.
    Custom,

    /// Any other value not defined in `TargetRollingWindowSizeMode`.
    UnknownValue(String),
}

/// AutoMLJob Task type.
#[derive(Clone, Debug, Eq, PartialEq)]
pub enum TaskType {
    /// Classification in machine learning and statistics is a supervised learning approach in which
    /// the computer program learns from the data given to it and make new observations or classifications.
    Classification,

    /// Forecasting is a special kind of regression task that deals with time-series data and creates forecasting model
    /// that can be used to predict the near future values based on the inputs.
    Forecasting,

    /// Image Classification. Multi-class image classification is used when an image is classified with only a single label
    /// from a set of classes - e.g. each image is classified as either an image of a 'cat' or a 'dog' or a 'duck'.
    ImageClassification,

    /// Image Classification Multilabel. Multi-label image classification is used when an image could have one or more labels
    /// from a set of labels - e.g. an image could be labeled with both 'cat' and 'dog'.
    ImageClassificationMultilabel,

    /// Image Instance Segmentation. Instance segmentation is used to identify objects in an image at the pixel level,
    /// drawing a polygon around each object in the image.
    ImageInstanceSegmentation,

    /// Image Object Detection. Object detection is used to identify objects in an image and locate each object with a
    /// bounding box e.g. locate all dogs and cats in an image and draw a bounding box around each.
    ImageObjectDetection,

    /// Regression means to predict the value using the input data. Regression models are used to predict a continuous value.
    Regression,

    /// Text classification (also known as text tagging or text categorization) is the process of sorting texts into categories.
    /// Categories are mutually exclusive.
    TextClassification,

    /// Multilabel classification task assigns each sample to a group (zero or more) of target labels.
    TextClassificationMultilabel,

    /// Text Named Entity Recognition a.k.a. TextNER.
    /// Named Entity Recognition (NER) is the ability to take free-form text and identify the occurrences of entities such as
    /// people, locations, organizations, and more.
    TextNer,

    /// Any other value not defined in `TaskType`.
    UnknownValue(String),
}

#[derive(Clone, Debug, Eq, PartialEq)]
pub enum TriggerType {
    Cron,

    Recurrence,

    /// Any other value not defined in `TriggerType`.
    UnknownValue(String),
}

#[derive(Clone, Debug, Eq, PartialEq)]
pub enum UnderlyingResourceAction {
    Delete,

    Detach,

    /// Any other value not defined in `UnderlyingResourceAction`.
    UnknownValue(String),
}

/// The unit of time measurement for the specified VM price. Example: OneHour
#[derive(Clone, Debug, Eq, PartialEq)]
pub enum UnitOfMeasure {
    OneHour,

    /// Any other value not defined in `UnitOfMeasure`.
    UnknownValue(String),
}

/// An enum describing the unit of usage measurement.
#[derive(Clone, Debug, Eq, PartialEq)]
pub enum UsageUnit {
    Count,

    /// Any other value not defined in `UsageUnit`.
    UnknownValue(String),
}

/// Configure STL Decomposition of the time-series target column.
#[derive(Clone, Debug, Eq, PartialEq)]
pub enum UseStl {
    /// No stl decomposition.
    None,

    Season,

    SeasonTrend,

    /// Any other value not defined in `UseStl`.
    UnknownValue(String),
}

/// Operating system type used by the VM.
#[derive(Clone, Debug, Eq, PartialEq)]
pub enum VMPriceOstype {
    Linux,

    Windows,

    /// Any other value not defined in `VMPriceOstype`.
    UnknownValue(String),
}

/// Virtual Machine priority
#[derive(Clone, Debug, Eq, PartialEq)]
pub enum VMPriority {
    Dedicated,

    LowPriority,

    /// Any other value not defined in `VMPriority`.
    UnknownValue(String),
}

/// The type of the VM.
#[derive(Clone, Debug, Eq, PartialEq)]
pub enum VMTier {
    LowPriority,

    Spot,

    Standard,

    /// Any other value not defined in `VMTier`.
    UnknownValue(String),
}

/// Metric computation method to use for validation metrics in image tasks.
#[derive(Clone, Debug, Eq, PartialEq)]
pub enum ValidationMetricType {
    /// Coco metric.
    Coco,

    /// CocoVoc metric.
    CocoVoc,

    /// No metric.
    None,

    /// Voc metric.
    Voc,

    /// Any other value not defined in `ValidationMetricType`.
    UnknownValue(String),
}

/// Type of Volume Definition. Possible Values: bind,volume,tmpfs,npipe
#[derive(Clone, Debug, Eq, PartialEq)]
pub enum VolumeDefinitionType {
    Bind,

    Npipe,

    Tmpfs,

    Volume,

    /// Any other value not defined in `VolumeDefinitionType`.
    UnknownValue(String),
}

/// Enum to determine the webhook callback service type.
#[derive(Clone, Debug, Eq, PartialEq)]
pub enum WebhookType {
    AzureDevOps,

    /// Any other value not defined in `WebhookType`.
    UnknownValue(String),
}

/// Enum of weekday
#[derive(Clone, Debug, Eq, PartialEq)]
pub enum WeekDay {
    /// Friday weekday
    Friday,

    /// Monday weekday
    Monday,

    /// Saturday weekday
    Saturday,

    /// Sunday weekday
    Sunday,

    /// Thursday weekday
    Thursday,

    /// Tuesday weekday
    Tuesday,

    /// Wednesday weekday
    Wednesday,

    /// Any other value not defined in `WeekDay`.
    UnknownValue(String),
}

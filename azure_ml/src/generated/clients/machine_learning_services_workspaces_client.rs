// Copyright (c) Microsoft Corporation. All rights reserved.
// Licensed under the MIT License.
//
// Code generated by Microsoft (R) Rust Code Generator. DO NOT EDIT.

use crate::generated::models::{
    ExternalFQDNResponse, ListNotebookKeysResult, ListStorageAccountKeysResult,
    ListWorkspaceKeysResult, MachineLearningServicesWorkspacesClientCreateOrUpdateOperationStatus,
    MachineLearningServicesWorkspacesClientCreateOrUpdateOptions,
    MachineLearningServicesWorkspacesClientDeleteOperationStatus,
    MachineLearningServicesWorkspacesClientDeleteOptions,
    MachineLearningServicesWorkspacesClientDiagnoseOperationStatus,
    MachineLearningServicesWorkspacesClientDiagnoseOptions,
    MachineLearningServicesWorkspacesClientGetOptions,
    MachineLearningServicesWorkspacesClientListByResourceGroupOptions,
    MachineLearningServicesWorkspacesClientListBySubscriptionOptions,
    MachineLearningServicesWorkspacesClientListKeysOptions,
    MachineLearningServicesWorkspacesClientListNotebookAccessTokenOptions,
    MachineLearningServicesWorkspacesClientListNotebookKeysOptions,
    MachineLearningServicesWorkspacesClientListOutboundNetworkDependenciesEndpointsOptions,
    MachineLearningServicesWorkspacesClientListStorageAccountKeysOptions,
    MachineLearningServicesWorkspacesClientPrepareNotebookOperationStatus,
    MachineLearningServicesWorkspacesClientPrepareNotebookOptions,
    MachineLearningServicesWorkspacesClientResyncKeysOperationStatus,
    MachineLearningServicesWorkspacesClientResyncKeysOptions,
    MachineLearningServicesWorkspacesClientUpdateOperationStatus,
    MachineLearningServicesWorkspacesClientUpdateOptions, NotebookAccessTokenResult, Workspace,
    WorkspaceListResult, WorkspaceUpdateParameters,
};
use azure_core::{
    error::{CheckSuccessOptions, Error, ErrorKind},
    http::{
        headers::{HeaderName, RETRY_AFTER, RETRY_AFTER_MS, X_MS_RETRY_AFTER_MS},
        pager::{PagerContinuation, PagerResult, PagerState},
        poller::{
            get_retry_after, PollerContinuation, PollerResult, PollerState, PollerStatus,
            StatusMonitor,
        },
        Method, Pager, Pipeline, PipelineSendOptions, Poller, RawResponse, Request, RequestContent,
        Response, StatusCode, Url, UrlExt,
    },
    json, tracing, Result,
};

#[tracing::client]
pub struct MachineLearningServicesWorkspacesClient {
    pub(crate) api_version: String,
    pub(crate) endpoint: Url,
    pub(crate) pipeline: Pipeline,
    pub(crate) subscription_id: String,
}

impl MachineLearningServicesWorkspacesClient {
    /// Returns the Url associated with this client.
    pub fn endpoint(&self) -> &Url {
        &self.endpoint
    }

    /// Creates or updates a workspace with the specified parameters.
    ///
    /// Creates or updates a workspace with the specified parameters.
    ///
    /// # Arguments
    ///
    /// * `resource_group_name` - The name of the resource group. The name is case insensitive.
    /// * `workspace_name` - Azure Machine Learning Workspace Name
    /// * `body` - The parameters for creating or updating a machine learning workspace.
    /// * `options` - Optional parameters for the request.
    ///
    /// ## Response Headers
    ///
    /// The returned [`Response`](azure_core::http::Response) implements the [`MachineLearningServicesWorkspacesClientCreateOrUpdateOperationStatusHeaders`] trait, which provides
    /// access to response headers. For example:
    ///
    /// ```no_run
    /// use azure_core::{Result, http::Response};
    /// use azure_mgmt_machinelearning::models::{MachineLearningServicesWorkspacesClientCreateOrUpdateOperationStatus, MachineLearningServicesWorkspacesClientCreateOrUpdateOperationStatusHeaders};
    /// async fn example() -> Result<()> {
    ///     let response: Response<MachineLearningServicesWorkspacesClientCreateOrUpdateOperationStatus> = unimplemented!();
    ///     // Access response headers
    ///     if let Some(location) = response.location()? {
    ///         println!("location: {:?}", location);
    ///     }
    ///     if let Some(retry_after) = response.retry_after()? {
    ///         println!("retry-after: {:?}", retry_after);
    ///     }
    ///     Ok(())
    /// }
    /// ```
    ///
    /// ### Available headers
    /// * [`location`()](crate::generated::models::MachineLearningServicesWorkspacesClientCreateOrUpdateOperationStatusHeaders::location) - location
    /// * [`retry_after`()](crate::generated::models::MachineLearningServicesWorkspacesClientCreateOrUpdateOperationStatusHeaders::retry_after) - retry-after
    ///
    /// [`MachineLearningServicesWorkspacesClientCreateOrUpdateOperationStatusHeaders`]: crate::generated::models::MachineLearningServicesWorkspacesClientCreateOrUpdateOperationStatusHeaders
    #[tracing::function("Microsoft.MachineLearningServices.Workspaces.createOrUpdate")]
    pub fn create_or_update(
        &self,
        resource_group_name: &str,
        workspace_name: &str,
        body: RequestContent<Workspace>,
        options: Option<MachineLearningServicesWorkspacesClientCreateOrUpdateOptions<'_>>,
    ) -> Result<Poller<MachineLearningServicesWorkspacesClientCreateOrUpdateOperationStatus>> {
        let options = options.unwrap_or_default().into_owned();
        let pipeline = self.pipeline.clone();
        let mut url = self.endpoint.clone();
        let mut path = String::from("/subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/Microsoft.MachineLearningServices/workspaces/{workspaceName}");
        path = path.replace("{resourceGroupName}", resource_group_name);
        path = path.replace("{subscriptionId}", &self.subscription_id);
        path = path.replace("{workspaceName}", workspace_name);
        url.append_path(&path);
        let mut query_builder = url.query_builder();
        query_builder.set_pair("api-version", &self.api_version);
        query_builder.build();
        let api_version = self.api_version.clone();
        Ok(Poller::new(
            move |poller_state: PollerState, poller_options| {
                let (mut request, continuation) = match poller_state {
                    PollerState::More(continuation) => {
                        let (mut next_link, final_link) = match continuation {
                            PollerContinuation::Links {
                                next_link,
                                final_link,
                            } => (next_link, final_link),
                            _ => {
                                unreachable!()
                            }
                        };
                        let mut query_builder = next_link.query_builder();
                        query_builder.set_pair("api-version", &api_version);
                        query_builder.build();
                        let mut request = Request::new(next_link.clone(), Method::Get);
                        request.insert_header("accept", "application/json");
                        request.insert_header("content-type", "application/json");
                        (
                            request,
                            PollerContinuation::Links {
                                next_link,
                                final_link,
                            },
                        )
                    }
                    PollerState::Initial => {
                        let mut request = Request::new(url.clone(), Method::Put);
                        request.insert_header("accept", "application/json");
                        request.insert_header("content-type", "application/json");
                        request.set_body(body.clone());
                        (
                            request,
                            PollerContinuation::Links {
                                next_link: url.clone(),
                                final_link: None,
                            },
                        )
                    }
                };
                let ctx = poller_options.context.clone();
                let pipeline = pipeline.clone();
                Box::pin(async move {
                    let rsp = pipeline
                        .send(
                            &ctx,
                            &mut request,
                            Some(PipelineSendOptions {
                                check_success: CheckSuccessOptions {
                                    success_codes: &[200, 202],
                                },
                                ..Default::default()
                            }),
                        )
                        .await?;
                    let (status, headers, body) = rsp.deconstruct();
                    let continuation = if let Some(operation_location) =
                        headers.get_optional_string(&HeaderName::from_static("location"))
                    {
                        let next_link = Url::parse(&operation_location)?;
                        match continuation {
                            PollerContinuation::Links { final_link, .. } => {
                                PollerContinuation::Links {
                                    next_link,
                                    final_link,
                                }
                            }
                            _ => {
                                unreachable!()
                            }
                        }
                    } else {
                        continuation
                    };
                    let retry_after = get_retry_after(
                        &headers,
                        &[X_MS_RETRY_AFTER_MS, RETRY_AFTER_MS, RETRY_AFTER],
                        &poller_options,
                    );
                    let res: MachineLearningServicesWorkspacesClientCreateOrUpdateOperationStatus =
                        json::from_json(&body)?;
                    let mut final_rsp: Option<RawResponse> = None;
                    if res.status() == PollerStatus::Succeeded {
                        final_rsp = Some(RawResponse::from_bytes(
                            status,
                            headers.clone(),
                            body.clone(),
                        ));
                    }
                    let rsp = RawResponse::from_bytes(status, headers, body).into();
                    Ok(match res.status() {
                        PollerStatus::InProgress => PollerResult::InProgress {
                            response: rsp,
                            retry_after,
                            continuation,
                        },
                        PollerStatus::Succeeded => PollerResult::Succeeded {
                            response: rsp,
                            target: Box::new(move || {
                                Box::pin(async move {
                                    Ok(final_rsp
                                        .ok_or_else(|| {
                                            Error::new(ErrorKind::Other, "missing final response")
                                        })?
                                        .into())
                                })
                            }),
                        },
                        _ => PollerResult::Done { response: rsp },
                    })
                })
            },
            Some(options.method_options),
        ))
    }

    /// Deletes a machine learning workspace.
    ///
    /// Deletes a machine learning workspace.
    ///
    /// # Arguments
    ///
    /// * `resource_group_name` - The name of the resource group. The name is case insensitive.
    /// * `workspace_name` - Azure Machine Learning Workspace Name
    /// * `options` - Optional parameters for the request.
    ///
    /// ## Response Headers
    ///
    /// The returned [`Response`](azure_core::http::Response) implements the [`MachineLearningServicesWorkspacesClientDeleteOperationStatusHeaders`] trait, which provides
    /// access to response headers. For example:
    ///
    /// ```no_run
    /// use azure_core::{Result, http::Response};
    /// use azure_mgmt_machinelearning::models::{MachineLearningServicesWorkspacesClientDeleteOperationStatus, MachineLearningServicesWorkspacesClientDeleteOperationStatusHeaders};
    /// async fn example() -> Result<()> {
    ///     let response: Response<MachineLearningServicesWorkspacesClientDeleteOperationStatus> = unimplemented!();
    ///     // Access response headers
    ///     if let Some(location) = response.location()? {
    ///         println!("location: {:?}", location);
    ///     }
    ///     if let Some(retry_after) = response.retry_after()? {
    ///         println!("retry-after: {:?}", retry_after);
    ///     }
    ///     Ok(())
    /// }
    /// ```
    ///
    /// ### Available headers
    /// * [`location`()](crate::generated::models::MachineLearningServicesWorkspacesClientDeleteOperationStatusHeaders::location) - location
    /// * [`retry_after`()](crate::generated::models::MachineLearningServicesWorkspacesClientDeleteOperationStatusHeaders::retry_after) - retry-after
    ///
    /// [`MachineLearningServicesWorkspacesClientDeleteOperationStatusHeaders`]: crate::generated::models::MachineLearningServicesWorkspacesClientDeleteOperationStatusHeaders
    #[tracing::function("Microsoft.MachineLearningServices.Workspaces.delete")]
    pub fn delete(
        &self,
        resource_group_name: &str,
        workspace_name: &str,
        options: Option<MachineLearningServicesWorkspacesClientDeleteOptions<'_>>,
    ) -> Result<Poller<MachineLearningServicesWorkspacesClientDeleteOperationStatus>> {
        let options = options.unwrap_or_default().into_owned();
        let pipeline = self.pipeline.clone();
        let mut url = self.endpoint.clone();
        let mut path = String::from("/subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/Microsoft.MachineLearningServices/workspaces/{workspaceName}");
        path = path.replace("{resourceGroupName}", resource_group_name);
        path = path.replace("{subscriptionId}", &self.subscription_id);
        path = path.replace("{workspaceName}", workspace_name);
        url.append_path(&path);
        let mut query_builder = url.query_builder();
        query_builder.set_pair("api-version", &self.api_version);
        if let Some(force_to_purge) = options.force_to_purge {
            query_builder.set_pair("forceToPurge", force_to_purge.to_string());
        }
        query_builder.build();
        let api_version = self.api_version.clone();
        Ok(Poller::new(
            move |poller_state: PollerState, poller_options| {
                let (mut request, continuation) = match poller_state {
                    PollerState::More(continuation) => {
                        let (mut next_link, final_link) = match continuation.clone() {
                            PollerContinuation::Links {
                                next_link,
                                final_link,
                            } => (next_link, final_link),
                            _ => {
                                unreachable!()
                            }
                        };
                        let mut query_builder = next_link.query_builder();
                        query_builder.set_pair("api-version", &api_version);
                        query_builder.build();
                        let request = Request::new(next_link.clone(), Method::Get);
                        (
                            request,
                            PollerContinuation::Links {
                                next_link,
                                final_link,
                            },
                        )
                    }
                    PollerState::Initial => {
                        let request = Request::new(url.clone(), Method::Delete);
                        (
                            request,
                            PollerContinuation::Links {
                                next_link: url.clone(),
                                final_link: None,
                            },
                        )
                    }
                };
                let ctx = poller_options.context.clone();
                let pipeline = pipeline.clone();
                Box::pin(async move {
                    let rsp = pipeline
                        .send(
                            &ctx,
                            &mut request,
                            Some(PipelineSendOptions {
                                check_success: CheckSuccessOptions {
                                    success_codes: &[200, 202, 204],
                                },
                                ..Default::default()
                            }),
                        )
                        .await?;
                    let (status, headers, mut body) = rsp.deconstruct();
                    if body.is_empty() {
                        body = azure_core::http::response::ResponseBody::from_bytes(
                            if status == StatusCode::NoContent {
                                "{\"status\":\"Succeeded\"}"
                            } else {
                                "{}"
                            },
                        );
                    }
                    let continuation = if let Some(operation_location) =
                        headers.get_optional_string(&HeaderName::from_static("location"))
                    {
                        let next_link = Url::parse(&operation_location)?;
                        match continuation {
                            PollerContinuation::Links { final_link, .. } => {
                                PollerContinuation::Links {
                                    next_link,
                                    final_link,
                                }
                            }
                            _ => {
                                unreachable!()
                            }
                        }
                    } else {
                        continuation
                    };
                    let retry_after = get_retry_after(
                        &headers,
                        &[X_MS_RETRY_AFTER_MS, RETRY_AFTER_MS, RETRY_AFTER],
                        &poller_options,
                    );
                    let res: MachineLearningServicesWorkspacesClientDeleteOperationStatus =
                        json::from_json(&body)?;
                    let mut final_rsp: Option<RawResponse> = None;
                    if res.status() == PollerStatus::Succeeded {
                        final_rsp = Some(RawResponse::from_bytes(
                            status,
                            headers.clone(),
                            body.clone(),
                        ));
                    }
                    let rsp = RawResponse::from_bytes(status, headers, body).into();
                    Ok(match res.status() {
                        PollerStatus::InProgress => PollerResult::InProgress {
                            response: rsp,
                            retry_after,
                            continuation,
                        },
                        PollerStatus::Succeeded => PollerResult::Succeeded {
                            response: rsp,
                            target: Box::new(move || {
                                Box::pin(async move {
                                    Ok(final_rsp
                                        .ok_or_else(|| {
                                            Error::new(ErrorKind::Other, "missing final response")
                                        })?
                                        .into())
                                })
                            }),
                        },
                        _ => PollerResult::Done { response: rsp },
                    })
                })
            },
            Some(options.method_options),
        ))
    }

    /// Diagnose workspace setup issue.
    ///
    /// Diagnose workspace setup issue.
    ///
    /// # Arguments
    ///
    /// * `resource_group_name` - The name of the resource group. The name is case insensitive.
    /// * `workspace_name` - Azure Machine Learning Workspace Name
    /// * `options` - Optional parameters for the request.
    ///
    /// ## Response Headers
    ///
    /// The returned [`Response`](azure_core::http::Response) implements the [`MachineLearningServicesWorkspacesClientDiagnoseOperationStatusHeaders`] trait, which provides
    /// access to response headers. For example:
    ///
    /// ```no_run
    /// use azure_core::{Result, http::Response};
    /// use azure_mgmt_machinelearning::models::{MachineLearningServicesWorkspacesClientDiagnoseOperationStatus, MachineLearningServicesWorkspacesClientDiagnoseOperationStatusHeaders};
    /// async fn example() -> Result<()> {
    ///     let response: Response<MachineLearningServicesWorkspacesClientDiagnoseOperationStatus> = unimplemented!();
    ///     // Access response headers
    ///     if let Some(location) = response.location()? {
    ///         println!("location: {:?}", location);
    ///     }
    ///     if let Some(retry_after) = response.retry_after()? {
    ///         println!("retry-after: {:?}", retry_after);
    ///     }
    ///     Ok(())
    /// }
    /// ```
    ///
    /// ### Available headers
    /// * [`location`()](crate::generated::models::MachineLearningServicesWorkspacesClientDiagnoseOperationStatusHeaders::location) - location
    /// * [`retry_after`()](crate::generated::models::MachineLearningServicesWorkspacesClientDiagnoseOperationStatusHeaders::retry_after) - retry-after
    ///
    /// [`MachineLearningServicesWorkspacesClientDiagnoseOperationStatusHeaders`]: crate::generated::models::MachineLearningServicesWorkspacesClientDiagnoseOperationStatusHeaders
    #[tracing::function("Microsoft.MachineLearningServices.Workspaces.diagnose")]
    pub fn diagnose(
        &self,
        resource_group_name: &str,
        workspace_name: &str,
        options: Option<MachineLearningServicesWorkspacesClientDiagnoseOptions<'_>>,
    ) -> Result<Poller<MachineLearningServicesWorkspacesClientDiagnoseOperationStatus>> {
        let options = options.unwrap_or_default().into_owned();
        let pipeline = self.pipeline.clone();
        let mut url = self.endpoint.clone();
        let mut path = String::from("/subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/Microsoft.MachineLearningServices/workspaces/{workspaceName}/diagnose");
        path = path.replace("{resourceGroupName}", resource_group_name);
        path = path.replace("{subscriptionId}", &self.subscription_id);
        path = path.replace("{workspaceName}", workspace_name);
        url.append_path(&path);
        let mut query_builder = url.query_builder();
        query_builder.set_pair("api-version", &self.api_version);
        query_builder.build();
        let api_version = self.api_version.clone();
        Ok(Poller::new(
            move |poller_state: PollerState, poller_options| {
                let (mut request, continuation) = match poller_state {
                    PollerState::More(continuation) => {
                        let (mut next_link, final_link) = match continuation {
                            PollerContinuation::Links {
                                next_link,
                                final_link,
                            } => (next_link, final_link),
                            _ => {
                                unreachable!()
                            }
                        };
                        let mut query_builder = next_link.query_builder();
                        query_builder.set_pair("api-version", &api_version);
                        query_builder.build();
                        let mut request = Request::new(next_link.clone(), Method::Get);
                        request.insert_header("accept", "application/json");
                        (
                            request,
                            PollerContinuation::Links {
                                next_link,
                                final_link,
                            },
                        )
                    }
                    PollerState::Initial => {
                        let mut request = Request::new(url.clone(), Method::Post);
                        request.insert_header("accept", "application/json");
                        if let Some(body) = options.body.clone() {
                            request.insert_header("content-type", "application/json");
                            request.set_body(body.clone());
                        }
                        (
                            request,
                            PollerContinuation::Links {
                                next_link: url.clone(),
                                final_link: None,
                            },
                        )
                    }
                };
                let ctx = poller_options.context.clone();
                let pipeline = pipeline.clone();
                Box::pin(async move {
                    let rsp = pipeline
                        .send(
                            &ctx,
                            &mut request,
                            Some(PipelineSendOptions {
                                check_success: CheckSuccessOptions {
                                    success_codes: &[200, 202],
                                },
                                ..Default::default()
                            }),
                        )
                        .await?;
                    let (status, headers, body) = rsp.deconstruct();
                    let continuation = if let Some(operation_location) =
                        headers.get_optional_string(&HeaderName::from_static("location"))
                    {
                        let next_link = Url::parse(&operation_location)?;
                        match continuation {
                            PollerContinuation::Links { final_link, .. } => {
                                PollerContinuation::Links {
                                    next_link,
                                    final_link,
                                }
                            }
                            _ => {
                                unreachable!()
                            }
                        }
                    } else {
                        continuation
                    };
                    let retry_after = get_retry_after(
                        &headers,
                        &[X_MS_RETRY_AFTER_MS, RETRY_AFTER_MS, RETRY_AFTER],
                        &poller_options,
                    );
                    let res: MachineLearningServicesWorkspacesClientDiagnoseOperationStatus =
                        json::from_json(&body)?;
                    let mut final_rsp: Option<RawResponse> = None;
                    if res.status() == PollerStatus::Succeeded {
                        final_rsp = Some(RawResponse::from_bytes(
                            status,
                            headers.clone(),
                            body.clone(),
                        ));
                    }
                    let rsp = RawResponse::from_bytes(status, headers, body).into();
                    Ok(match res.status() {
                        PollerStatus::InProgress => PollerResult::InProgress {
                            response: rsp,
                            retry_after,
                            continuation,
                        },
                        PollerStatus::Succeeded => PollerResult::Succeeded {
                            response: rsp,
                            target: Box::new(move || {
                                Box::pin(async move {
                                    Ok(final_rsp
                                        .ok_or_else(|| {
                                            Error::new(ErrorKind::Other, "missing final response")
                                        })?
                                        .into())
                                })
                            }),
                        },
                        _ => PollerResult::Done { response: rsp },
                    })
                })
            },
            Some(options.method_options),
        ))
    }

    /// Gets the properties of the specified machine learning workspace.
    ///
    /// Gets the properties of the specified machine learning workspace.
    ///
    /// # Arguments
    ///
    /// * `resource_group_name` - The name of the resource group. The name is case insensitive.
    /// * `workspace_name` - Azure Machine Learning Workspace Name
    /// * `options` - Optional parameters for the request.
    #[tracing::function("Microsoft.MachineLearningServices.Workspaces.get")]
    pub async fn get(
        &self,
        resource_group_name: &str,
        workspace_name: &str,
        options: Option<MachineLearningServicesWorkspacesClientGetOptions<'_>>,
    ) -> Result<Response<Workspace>> {
        if resource_group_name.is_empty() {
            return Err(azure_core::Error::with_message(
                azure_core::error::ErrorKind::Other,
                "parameter resource_group_name cannot be empty",
            ));
        }
        if workspace_name.is_empty() {
            return Err(azure_core::Error::with_message(
                azure_core::error::ErrorKind::Other,
                "parameter workspace_name cannot be empty",
            ));
        }
        let options = options.unwrap_or_default();
        let ctx = options.method_options.context.to_borrowed();
        let mut url = self.endpoint.clone();
        let mut path = String::from("/subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/Microsoft.MachineLearningServices/workspaces/{workspaceName}");
        path = path.replace("{resourceGroupName}", resource_group_name);
        path = path.replace("{subscriptionId}", &self.subscription_id);
        path = path.replace("{workspaceName}", workspace_name);
        url.append_path(&path);
        let mut query_builder = url.query_builder();
        query_builder.set_pair("api-version", &self.api_version);
        query_builder.build();
        let mut request = Request::new(url, Method::Get);
        request.insert_header("accept", "application/json");
        let rsp = self
            .pipeline
            .send(
                &ctx,
                &mut request,
                Some(PipelineSendOptions {
                    check_success: CheckSuccessOptions {
                        success_codes: &[200],
                    },
                    ..Default::default()
                }),
            )
            .await?;
        Ok(rsp.into())
    }

    /// Lists all the available machine learning workspaces under the specified resource group.
    ///
    /// Lists all the available machine learning workspaces under the specified resource group.
    ///
    /// # Arguments
    ///
    /// * `resource_group_name` - The name of the resource group. The name is case insensitive.
    /// * `options` - Optional parameters for the request.
    #[tracing::function("Microsoft.MachineLearningServices.Workspaces.listByResourceGroup")]
    pub fn list_by_resource_group(
        &self,
        resource_group_name: &str,
        options: Option<MachineLearningServicesWorkspacesClientListByResourceGroupOptions<'_>>,
    ) -> Result<Pager<WorkspaceListResult>> {
        if resource_group_name.is_empty() {
            return Err(azure_core::Error::with_message(
                azure_core::error::ErrorKind::Other,
                "parameter resource_group_name cannot be empty",
            ));
        }
        let options = options.unwrap_or_default().into_owned();
        let pipeline = self.pipeline.clone();
        let mut first_url = self.endpoint.clone();
        let mut path = String::from("/subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/Microsoft.MachineLearningServices/workspaces");
        path = path.replace("{resourceGroupName}", resource_group_name);
        path = path.replace("{subscriptionId}", &self.subscription_id);
        first_url.append_path(&path);
        let mut query_builder = first_url.query_builder();
        if let Some(skip) = options.skip.as_ref() {
            query_builder.set_pair("$skip", skip);
        }
        if let Some(ai_capabilities) = options.ai_capabilities.as_ref() {
            query_builder.set_pair("aiCapabilities", ai_capabilities);
        }
        query_builder.set_pair("api-version", &self.api_version);
        if let Some(kind) = options.kind.as_ref() {
            query_builder.set_pair("kind", kind);
        }
        query_builder.build();
        let api_version = self.api_version.clone();
        Ok(Pager::new(
            move |next_link: PagerState, pager_options| {
                let url = match next_link {
                    PagerState::More(next_link) => {
                        let mut next_link: Url = next_link.try_into().expect("expected Url");
                        let mut query_builder = next_link.query_builder();
                        query_builder.set_pair("api-version", &api_version);
                        query_builder.build();
                        next_link
                    }
                    PagerState::Initial => first_url.clone(),
                };
                let mut request = Request::new(url, Method::Get);
                request.insert_header("accept", "application/json");
                let pipeline = pipeline.clone();
                Box::pin(async move {
                    let rsp = pipeline
                        .send(
                            &pager_options.context,
                            &mut request,
                            Some(PipelineSendOptions {
                                check_success: CheckSuccessOptions {
                                    success_codes: &[200],
                                },
                                ..Default::default()
                            }),
                        )
                        .await?;
                    let (status, headers, body) = rsp.deconstruct();
                    let res: WorkspaceListResult = json::from_json(&body)?;
                    let rsp = RawResponse::from_bytes(status, headers, body).into();
                    Ok(match res.next_link {
                        Some(next_link) if !next_link.is_empty() => PagerResult::More {
                            response: rsp,
                            continuation: PagerContinuation::Link(next_link.parse()?),
                        },
                        _ => PagerResult::Done { response: rsp },
                    })
                })
            },
            Some(options.method_options),
        ))
    }

    /// Lists all the available machine learning workspaces under the specified subscription.
    ///
    /// Lists all the available machine learning workspaces under the specified subscription.
    ///
    /// # Arguments
    ///
    /// * `options` - Optional parameters for the request.
    #[tracing::function("Microsoft.MachineLearningServices.Workspaces.listBySubscription")]
    pub fn list_by_subscription(
        &self,
        options: Option<MachineLearningServicesWorkspacesClientListBySubscriptionOptions<'_>>,
    ) -> Result<Pager<WorkspaceListResult>> {
        let options = options.unwrap_or_default().into_owned();
        let pipeline = self.pipeline.clone();
        let mut first_url = self.endpoint.clone();
        let mut path = String::from("/subscriptions/{subscriptionId}/providers/Microsoft.MachineLearningServices/workspaces");
        path = path.replace("{subscriptionId}", &self.subscription_id);
        first_url.append_path(&path);
        let mut query_builder = first_url.query_builder();
        if let Some(skip) = options.skip.as_ref() {
            query_builder.set_pair("$skip", skip);
        }
        if let Some(ai_capabilities) = options.ai_capabilities.as_ref() {
            query_builder.set_pair("aiCapabilities", ai_capabilities);
        }
        query_builder.set_pair("api-version", &self.api_version);
        if let Some(kind) = options.kind.as_ref() {
            query_builder.set_pair("kind", kind);
        }
        query_builder.build();
        let api_version = self.api_version.clone();
        Ok(Pager::new(
            move |next_link: PagerState, pager_options| {
                let url = match next_link {
                    PagerState::More(next_link) => {
                        let mut next_link: Url = next_link.try_into().expect("expected Url");
                        let mut query_builder = next_link.query_builder();
                        query_builder.set_pair("api-version", &api_version);
                        query_builder.build();
                        next_link
                    }
                    PagerState::Initial => first_url.clone(),
                };
                let mut request = Request::new(url, Method::Get);
                request.insert_header("accept", "application/json");
                let pipeline = pipeline.clone();
                Box::pin(async move {
                    let rsp = pipeline
                        .send(
                            &pager_options.context,
                            &mut request,
                            Some(PipelineSendOptions {
                                check_success: CheckSuccessOptions {
                                    success_codes: &[200],
                                },
                                ..Default::default()
                            }),
                        )
                        .await?;
                    let (status, headers, body) = rsp.deconstruct();
                    let res: WorkspaceListResult = json::from_json(&body)?;
                    let rsp = RawResponse::from_bytes(status, headers, body).into();
                    Ok(match res.next_link {
                        Some(next_link) if !next_link.is_empty() => PagerResult::More {
                            response: rsp,
                            continuation: PagerContinuation::Link(next_link.parse()?),
                        },
                        _ => PagerResult::Done { response: rsp },
                    })
                })
            },
            Some(options.method_options),
        ))
    }

    /// Lists all the keys associated with this workspace. This includes keys for the storage account, app insights and password
    /// for container registry.
    ///
    /// Lists all the keys associated with this workspace. This includes keys for the storage account, app insights and password
    /// for container registry.
    ///
    /// # Arguments
    ///
    /// * `resource_group_name` - The name of the resource group. The name is case insensitive.
    /// * `workspace_name` - Azure Machine Learning Workspace Name
    /// * `options` - Optional parameters for the request.
    #[tracing::function("Microsoft.MachineLearningServices.Workspaces.listKeys")]
    pub async fn list_keys(
        &self,
        resource_group_name: &str,
        workspace_name: &str,
        options: Option<MachineLearningServicesWorkspacesClientListKeysOptions<'_>>,
    ) -> Result<Response<ListWorkspaceKeysResult>> {
        if resource_group_name.is_empty() {
            return Err(azure_core::Error::with_message(
                azure_core::error::ErrorKind::Other,
                "parameter resource_group_name cannot be empty",
            ));
        }
        if workspace_name.is_empty() {
            return Err(azure_core::Error::with_message(
                azure_core::error::ErrorKind::Other,
                "parameter workspace_name cannot be empty",
            ));
        }
        let options = options.unwrap_or_default();
        let ctx = options.method_options.context.to_borrowed();
        let mut url = self.endpoint.clone();
        let mut path = String::from("/subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/Microsoft.MachineLearningServices/workspaces/{workspaceName}/listKeys");
        path = path.replace("{resourceGroupName}", resource_group_name);
        path = path.replace("{subscriptionId}", &self.subscription_id);
        path = path.replace("{workspaceName}", workspace_name);
        url.append_path(&path);
        let mut query_builder = url.query_builder();
        query_builder.set_pair("api-version", &self.api_version);
        query_builder.build();
        let mut request = Request::new(url, Method::Post);
        request.insert_header("accept", "application/json");
        let rsp = self
            .pipeline
            .send(
                &ctx,
                &mut request,
                Some(PipelineSendOptions {
                    check_success: CheckSuccessOptions {
                        success_codes: &[200],
                    },
                    ..Default::default()
                }),
            )
            .await?;
        Ok(rsp.into())
    }

    /// Get Azure Machine Learning Workspace notebook access token
    ///
    /// Get Azure Machine Learning Workspace notebook access token
    ///
    /// # Arguments
    ///
    /// * `resource_group_name` - The name of the resource group. The name is case insensitive.
    /// * `workspace_name` - Azure Machine Learning Workspace Name
    /// * `options` - Optional parameters for the request.
    #[tracing::function("Microsoft.MachineLearningServices.Workspaces.listNotebookAccessToken")]
    pub async fn list_notebook_access_token(
        &self,
        resource_group_name: &str,
        workspace_name: &str,
        options: Option<MachineLearningServicesWorkspacesClientListNotebookAccessTokenOptions<'_>>,
    ) -> Result<Response<NotebookAccessTokenResult>> {
        if resource_group_name.is_empty() {
            return Err(azure_core::Error::with_message(
                azure_core::error::ErrorKind::Other,
                "parameter resource_group_name cannot be empty",
            ));
        }
        if workspace_name.is_empty() {
            return Err(azure_core::Error::with_message(
                azure_core::error::ErrorKind::Other,
                "parameter workspace_name cannot be empty",
            ));
        }
        let options = options.unwrap_or_default();
        let ctx = options.method_options.context.to_borrowed();
        let mut url = self.endpoint.clone();
        let mut path = String::from("/subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/Microsoft.MachineLearningServices/workspaces/{workspaceName}/listNotebookAccessToken");
        path = path.replace("{resourceGroupName}", resource_group_name);
        path = path.replace("{subscriptionId}", &self.subscription_id);
        path = path.replace("{workspaceName}", workspace_name);
        url.append_path(&path);
        let mut query_builder = url.query_builder();
        query_builder.set_pair("api-version", &self.api_version);
        query_builder.build();
        let mut request = Request::new(url, Method::Post);
        request.insert_header("accept", "application/json");
        let rsp = self
            .pipeline
            .send(
                &ctx,
                &mut request,
                Some(PipelineSendOptions {
                    check_success: CheckSuccessOptions {
                        success_codes: &[200],
                    },
                    ..Default::default()
                }),
            )
            .await?;
        Ok(rsp.into())
    }

    /// Lists keys of Azure Machine Learning Workspaces notebook.
    ///
    /// Lists keys of Azure Machine Learning Workspaces notebook.
    ///
    /// # Arguments
    ///
    /// * `resource_group_name` - The name of the resource group. The name is case insensitive.
    /// * `workspace_name` - Azure Machine Learning Workspace Name
    /// * `options` - Optional parameters for the request.
    #[tracing::function("Microsoft.MachineLearningServices.Workspaces.listNotebookKeys")]
    pub async fn list_notebook_keys(
        &self,
        resource_group_name: &str,
        workspace_name: &str,
        options: Option<MachineLearningServicesWorkspacesClientListNotebookKeysOptions<'_>>,
    ) -> Result<Response<ListNotebookKeysResult>> {
        if resource_group_name.is_empty() {
            return Err(azure_core::Error::with_message(
                azure_core::error::ErrorKind::Other,
                "parameter resource_group_name cannot be empty",
            ));
        }
        if workspace_name.is_empty() {
            return Err(azure_core::Error::with_message(
                azure_core::error::ErrorKind::Other,
                "parameter workspace_name cannot be empty",
            ));
        }
        let options = options.unwrap_or_default();
        let ctx = options.method_options.context.to_borrowed();
        let mut url = self.endpoint.clone();
        let mut path = String::from("/subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/Microsoft.MachineLearningServices/workspaces/{workspaceName}/listNotebookKeys");
        path = path.replace("{resourceGroupName}", resource_group_name);
        path = path.replace("{subscriptionId}", &self.subscription_id);
        path = path.replace("{workspaceName}", workspace_name);
        url.append_path(&path);
        let mut query_builder = url.query_builder();
        query_builder.set_pair("api-version", &self.api_version);
        query_builder.build();
        let mut request = Request::new(url, Method::Post);
        request.insert_header("accept", "application/json");
        let rsp = self
            .pipeline
            .send(
                &ctx,
                &mut request,
                Some(PipelineSendOptions {
                    check_success: CheckSuccessOptions {
                        success_codes: &[200],
                    },
                    ..Default::default()
                }),
            )
            .await?;
        Ok(rsp.into())
    }

    /// Called by Client (Portal, CLI, etc) to get a list of all external outbound dependencies (FQDNs) programmatically.
    ///
    /// Called by Client (Portal, CLI, etc) to get a list of all external outbound dependencies (FQDNs) programmatically.
    ///
    /// # Arguments
    ///
    /// * `resource_group_name` - The name of the resource group. The name is case insensitive.
    /// * `workspace_name` - Azure Machine Learning Workspace Name
    /// * `options` - Optional parameters for the request.
    #[tracing::function(
        "Microsoft.MachineLearningServices.Workspaces.listOutboundNetworkDependenciesEndpoints"
    )]
    pub async fn list_outbound_network_dependencies_endpoints(
        &self,
        resource_group_name: &str,
        workspace_name: &str,
        options: Option<
            MachineLearningServicesWorkspacesClientListOutboundNetworkDependenciesEndpointsOptions<
                '_,
            >,
        >,
    ) -> Result<Response<ExternalFQDNResponse>> {
        if resource_group_name.is_empty() {
            return Err(azure_core::Error::with_message(
                azure_core::error::ErrorKind::Other,
                "parameter resource_group_name cannot be empty",
            ));
        }
        if workspace_name.is_empty() {
            return Err(azure_core::Error::with_message(
                azure_core::error::ErrorKind::Other,
                "parameter workspace_name cannot be empty",
            ));
        }
        let options = options.unwrap_or_default();
        let ctx = options.method_options.context.to_borrowed();
        let mut url = self.endpoint.clone();
        let mut path = String::from("/subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/Microsoft.MachineLearningServices/workspaces/{workspaceName}/outboundNetworkDependenciesEndpoints");
        path = path.replace("{resourceGroupName}", resource_group_name);
        path = path.replace("{subscriptionId}", &self.subscription_id);
        path = path.replace("{workspaceName}", workspace_name);
        url.append_path(&path);
        let mut query_builder = url.query_builder();
        query_builder.set_pair("api-version", &self.api_version);
        query_builder.build();
        let mut request = Request::new(url, Method::Get);
        request.insert_header("accept", "application/json");
        let rsp = self
            .pipeline
            .send(
                &ctx,
                &mut request,
                Some(PipelineSendOptions {
                    check_success: CheckSuccessOptions {
                        success_codes: &[200],
                    },
                    ..Default::default()
                }),
            )
            .await?;
        Ok(rsp.into())
    }

    /// Lists keys of Azure Machine Learning Workspace's storage account.
    ///
    /// Lists keys of Azure Machine Learning Workspace's storage account.
    ///
    /// # Arguments
    ///
    /// * `resource_group_name` - The name of the resource group. The name is case insensitive.
    /// * `workspace_name` - Azure Machine Learning Workspace Name
    /// * `options` - Optional parameters for the request.
    #[tracing::function("Microsoft.MachineLearningServices.Workspaces.listStorageAccountKeys")]
    pub async fn list_storage_account_keys(
        &self,
        resource_group_name: &str,
        workspace_name: &str,
        options: Option<MachineLearningServicesWorkspacesClientListStorageAccountKeysOptions<'_>>,
    ) -> Result<Response<ListStorageAccountKeysResult>> {
        if resource_group_name.is_empty() {
            return Err(azure_core::Error::with_message(
                azure_core::error::ErrorKind::Other,
                "parameter resource_group_name cannot be empty",
            ));
        }
        if workspace_name.is_empty() {
            return Err(azure_core::Error::with_message(
                azure_core::error::ErrorKind::Other,
                "parameter workspace_name cannot be empty",
            ));
        }
        let options = options.unwrap_or_default();
        let ctx = options.method_options.context.to_borrowed();
        let mut url = self.endpoint.clone();
        let mut path = String::from("/subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/Microsoft.MachineLearningServices/workspaces/{workspaceName}/listStorageAccountKeys");
        path = path.replace("{resourceGroupName}", resource_group_name);
        path = path.replace("{subscriptionId}", &self.subscription_id);
        path = path.replace("{workspaceName}", workspace_name);
        url.append_path(&path);
        let mut query_builder = url.query_builder();
        query_builder.set_pair("api-version", &self.api_version);
        query_builder.build();
        let mut request = Request::new(url, Method::Post);
        request.insert_header("accept", "application/json");
        let rsp = self
            .pipeline
            .send(
                &ctx,
                &mut request,
                Some(PipelineSendOptions {
                    check_success: CheckSuccessOptions {
                        success_codes: &[200],
                    },
                    ..Default::default()
                }),
            )
            .await?;
        Ok(rsp.into())
    }

    /// Prepare Azure Machine Learning Workspace's notebook resource
    ///
    /// Prepare Azure Machine Learning Workspace's notebook resource
    ///
    /// # Arguments
    ///
    /// * `resource_group_name` - The name of the resource group. The name is case insensitive.
    /// * `workspace_name` - Azure Machine Learning Workspace Name
    /// * `options` - Optional parameters for the request.
    ///
    /// ## Response Headers
    ///
    /// The returned [`Response`](azure_core::http::Response) implements the [`MachineLearningServicesWorkspacesClientPrepareNotebookOperationStatusHeaders`] trait, which provides
    /// access to response headers. For example:
    ///
    /// ```no_run
    /// use azure_core::{Result, http::Response};
    /// use azure_mgmt_machinelearning::models::{MachineLearningServicesWorkspacesClientPrepareNotebookOperationStatus, MachineLearningServicesWorkspacesClientPrepareNotebookOperationStatusHeaders};
    /// async fn example() -> Result<()> {
    ///     let response: Response<MachineLearningServicesWorkspacesClientPrepareNotebookOperationStatus> = unimplemented!();
    ///     // Access response headers
    ///     if let Some(location) = response.location()? {
    ///         println!("location: {:?}", location);
    ///     }
    ///     if let Some(retry_after) = response.retry_after()? {
    ///         println!("retry-after: {:?}", retry_after);
    ///     }
    ///     Ok(())
    /// }
    /// ```
    ///
    /// ### Available headers
    /// * [`location`()](crate::generated::models::MachineLearningServicesWorkspacesClientPrepareNotebookOperationStatusHeaders::location) - location
    /// * [`retry_after`()](crate::generated::models::MachineLearningServicesWorkspacesClientPrepareNotebookOperationStatusHeaders::retry_after) - retry-after
    ///
    /// [`MachineLearningServicesWorkspacesClientPrepareNotebookOperationStatusHeaders`]: crate::generated::models::MachineLearningServicesWorkspacesClientPrepareNotebookOperationStatusHeaders
    #[tracing::function("Microsoft.MachineLearningServices.Workspaces.prepareNotebook")]
    pub fn prepare_notebook(
        &self,
        resource_group_name: &str,
        workspace_name: &str,
        options: Option<MachineLearningServicesWorkspacesClientPrepareNotebookOptions<'_>>,
    ) -> Result<Poller<MachineLearningServicesWorkspacesClientPrepareNotebookOperationStatus>> {
        let options = options.unwrap_or_default().into_owned();
        let pipeline = self.pipeline.clone();
        let mut url = self.endpoint.clone();
        let mut path = String::from("/subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/Microsoft.MachineLearningServices/workspaces/{workspaceName}/prepareNotebook");
        path = path.replace("{resourceGroupName}", resource_group_name);
        path = path.replace("{subscriptionId}", &self.subscription_id);
        path = path.replace("{workspaceName}", workspace_name);
        url.append_path(&path);
        let mut query_builder = url.query_builder();
        query_builder.set_pair("api-version", &self.api_version);
        query_builder.build();
        let api_version = self.api_version.clone();
        Ok(Poller::new(
            move |poller_state: PollerState, poller_options| {
                let (mut request, continuation) = match poller_state {
                    PollerState::More(continuation) => {
                        let (mut next_link, final_link) = match continuation.clone() {
                            PollerContinuation::Links {
                                next_link,
                                final_link,
                            } => (next_link, final_link),
                            _ => {
                                unreachable!()
                            }
                        };
                        let mut query_builder = next_link.query_builder();
                        query_builder.set_pair("api-version", &api_version);
                        query_builder.build();
                        let mut request = Request::new(next_link.clone(), Method::Get);
                        request.insert_header("accept", "application/json");
                        (
                            request,
                            PollerContinuation::Links {
                                next_link,
                                final_link,
                            },
                        )
                    }
                    PollerState::Initial => {
                        let mut request = Request::new(url.clone(), Method::Post);
                        request.insert_header("accept", "application/json");
                        (
                            request,
                            PollerContinuation::Links {
                                next_link: url.clone(),
                                final_link: None,
                            },
                        )
                    }
                };
                let ctx = poller_options.context.clone();
                let pipeline = pipeline.clone();
                Box::pin(async move {
                    let rsp = pipeline
                        .send(
                            &ctx,
                            &mut request,
                            Some(PipelineSendOptions {
                                check_success: CheckSuccessOptions {
                                    success_codes: &[200, 202],
                                },
                                ..Default::default()
                            }),
                        )
                        .await?;
                    let (status, headers, body) = rsp.deconstruct();
                    let continuation = if let Some(operation_location) =
                        headers.get_optional_string(&HeaderName::from_static("location"))
                    {
                        let next_link = Url::parse(&operation_location)?;
                        match continuation {
                            PollerContinuation::Links { final_link, .. } => {
                                PollerContinuation::Links {
                                    next_link,
                                    final_link,
                                }
                            }
                            _ => {
                                unreachable!()
                            }
                        }
                    } else {
                        continuation
                    };
                    let retry_after = get_retry_after(
                        &headers,
                        &[X_MS_RETRY_AFTER_MS, RETRY_AFTER_MS, RETRY_AFTER],
                        &poller_options,
                    );
                    let res: MachineLearningServicesWorkspacesClientPrepareNotebookOperationStatus =
                        json::from_json(&body)?;
                    let mut final_rsp: Option<RawResponse> = None;
                    if res.status() == PollerStatus::Succeeded {
                        final_rsp = Some(RawResponse::from_bytes(
                            status,
                            headers.clone(),
                            body.clone(),
                        ));
                    }
                    let rsp = RawResponse::from_bytes(status, headers, body).into();
                    Ok(match res.status() {
                        PollerStatus::InProgress => PollerResult::InProgress {
                            response: rsp,
                            retry_after,
                            continuation,
                        },
                        PollerStatus::Succeeded => PollerResult::Succeeded {
                            response: rsp,
                            target: Box::new(move || {
                                Box::pin(async move {
                                    Ok(final_rsp
                                        .ok_or_else(|| {
                                            Error::new(ErrorKind::Other, "missing final response")
                                        })?
                                        .into())
                                })
                            }),
                        },
                        _ => PollerResult::Done { response: rsp },
                    })
                })
            },
            Some(options.method_options),
        ))
    }

    /// Resync all the keys associated with this workspace.This includes keys for the storage account, app insights and password
    /// for container registry
    ///
    /// Resync all the keys associated with this workspace.This includes keys for the storage account, app insights and password
    /// for container registry
    ///
    /// # Arguments
    ///
    /// * `resource_group_name` - The name of the resource group. The name is case insensitive.
    /// * `workspace_name` - Azure Machine Learning Workspace Name
    /// * `options` - Optional parameters for the request.
    ///
    /// ## Response Headers
    ///
    /// The returned [`Response`](azure_core::http::Response) implements the [`MachineLearningServicesWorkspacesClientResyncKeysOperationStatusHeaders`] trait, which provides
    /// access to response headers. For example:
    ///
    /// ```no_run
    /// use azure_core::{Result, http::Response};
    /// use azure_mgmt_machinelearning::models::{MachineLearningServicesWorkspacesClientResyncKeysOperationStatus, MachineLearningServicesWorkspacesClientResyncKeysOperationStatusHeaders};
    /// async fn example() -> Result<()> {
    ///     let response: Response<MachineLearningServicesWorkspacesClientResyncKeysOperationStatus> = unimplemented!();
    ///     // Access response headers
    ///     if let Some(location) = response.location()? {
    ///         println!("location: {:?}", location);
    ///     }
    ///     if let Some(retry_after) = response.retry_after()? {
    ///         println!("retry-after: {:?}", retry_after);
    ///     }
    ///     Ok(())
    /// }
    /// ```
    ///
    /// ### Available headers
    /// * [`location`()](crate::generated::models::MachineLearningServicesWorkspacesClientResyncKeysOperationStatusHeaders::location) - location
    /// * [`retry_after`()](crate::generated::models::MachineLearningServicesWorkspacesClientResyncKeysOperationStatusHeaders::retry_after) - retry-after
    ///
    /// [`MachineLearningServicesWorkspacesClientResyncKeysOperationStatusHeaders`]: crate::generated::models::MachineLearningServicesWorkspacesClientResyncKeysOperationStatusHeaders
    #[tracing::function("Microsoft.MachineLearningServices.Workspaces.resyncKeys")]
    pub fn resync_keys(
        &self,
        resource_group_name: &str,
        workspace_name: &str,
        options: Option<MachineLearningServicesWorkspacesClientResyncKeysOptions<'_>>,
    ) -> Result<Poller<MachineLearningServicesWorkspacesClientResyncKeysOperationStatus>> {
        let options = options.unwrap_or_default().into_owned();
        let pipeline = self.pipeline.clone();
        let mut url = self.endpoint.clone();
        let mut path = String::from("/subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/Microsoft.MachineLearningServices/workspaces/{workspaceName}/resyncKeys");
        path = path.replace("{resourceGroupName}", resource_group_name);
        path = path.replace("{subscriptionId}", &self.subscription_id);
        path = path.replace("{workspaceName}", workspace_name);
        url.append_path(&path);
        let mut query_builder = url.query_builder();
        query_builder.set_pair("api-version", &self.api_version);
        query_builder.build();
        let api_version = self.api_version.clone();
        Ok(Poller::new(
            move |poller_state: PollerState, poller_options| {
                let (mut request, continuation) = match poller_state {
                    PollerState::More(continuation) => {
                        let (mut next_link, final_link) = match continuation.clone() {
                            PollerContinuation::Links {
                                next_link,
                                final_link,
                            } => (next_link, final_link),
                            _ => {
                                unreachable!()
                            }
                        };
                        let mut query_builder = next_link.query_builder();
                        query_builder.set_pair("api-version", &api_version);
                        query_builder.build();
                        let request = Request::new(next_link.clone(), Method::Get);
                        (
                            request,
                            PollerContinuation::Links {
                                next_link,
                                final_link,
                            },
                        )
                    }
                    PollerState::Initial => {
                        let request = Request::new(url.clone(), Method::Post);
                        (
                            request,
                            PollerContinuation::Links {
                                next_link: url.clone(),
                                final_link: None,
                            },
                        )
                    }
                };
                let ctx = poller_options.context.clone();
                let pipeline = pipeline.clone();
                Box::pin(async move {
                    let rsp = pipeline
                        .send(
                            &ctx,
                            &mut request,
                            Some(PipelineSendOptions {
                                check_success: CheckSuccessOptions {
                                    success_codes: &[200, 202],
                                },
                                ..Default::default()
                            }),
                        )
                        .await?;
                    let (status, headers, body) = rsp.deconstruct();
                    let continuation = if let Some(operation_location) =
                        headers.get_optional_string(&HeaderName::from_static("location"))
                    {
                        let next_link = Url::parse(&operation_location)?;
                        match continuation {
                            PollerContinuation::Links { final_link, .. } => {
                                PollerContinuation::Links {
                                    next_link,
                                    final_link,
                                }
                            }
                            _ => {
                                unreachable!()
                            }
                        }
                    } else {
                        continuation
                    };
                    let retry_after = get_retry_after(
                        &headers,
                        &[X_MS_RETRY_AFTER_MS, RETRY_AFTER_MS, RETRY_AFTER],
                        &poller_options,
                    );
                    let res: MachineLearningServicesWorkspacesClientResyncKeysOperationStatus =
                        json::from_json(&body)?;
                    let mut final_rsp: Option<RawResponse> = None;
                    if res.status() == PollerStatus::Succeeded {
                        final_rsp = Some(RawResponse::from_bytes(
                            status,
                            headers.clone(),
                            body.clone(),
                        ));
                    }
                    let rsp = RawResponse::from_bytes(status, headers, body).into();
                    Ok(match res.status() {
                        PollerStatus::InProgress => PollerResult::InProgress {
                            response: rsp,
                            retry_after,
                            continuation,
                        },
                        PollerStatus::Succeeded => PollerResult::Succeeded {
                            response: rsp,
                            target: Box::new(move || {
                                Box::pin(async move {
                                    Ok(final_rsp
                                        .ok_or_else(|| {
                                            Error::new(ErrorKind::Other, "missing final response")
                                        })?
                                        .into())
                                })
                            }),
                        },
                        _ => PollerResult::Done { response: rsp },
                    })
                })
            },
            Some(options.method_options),
        ))
    }

    /// Updates a machine learning workspace with the specified parameters.
    ///
    /// Updates a machine learning workspace with the specified parameters.
    ///
    /// # Arguments
    ///
    /// * `resource_group_name` - The name of the resource group. The name is case insensitive.
    /// * `workspace_name` - Azure Machine Learning Workspace Name
    /// * `body` - The parameters for updating a machine learning workspace.
    /// * `options` - Optional parameters for the request.
    ///
    /// ## Response Headers
    ///
    /// The returned [`Response`](azure_core::http::Response) implements the [`MachineLearningServicesWorkspacesClientUpdateOperationStatusHeaders`] trait, which provides
    /// access to response headers. For example:
    ///
    /// ```no_run
    /// use azure_core::{Result, http::Response};
    /// use azure_mgmt_machinelearning::models::{MachineLearningServicesWorkspacesClientUpdateOperationStatus, MachineLearningServicesWorkspacesClientUpdateOperationStatusHeaders};
    /// async fn example() -> Result<()> {
    ///     let response: Response<MachineLearningServicesWorkspacesClientUpdateOperationStatus> = unimplemented!();
    ///     // Access response headers
    ///     if let Some(location) = response.location()? {
    ///         println!("location: {:?}", location);
    ///     }
    ///     if let Some(retry_after) = response.retry_after()? {
    ///         println!("retry-after: {:?}", retry_after);
    ///     }
    ///     Ok(())
    /// }
    /// ```
    ///
    /// ### Available headers
    /// * [`location`()](crate::generated::models::MachineLearningServicesWorkspacesClientUpdateOperationStatusHeaders::location) - location
    /// * [`retry_after`()](crate::generated::models::MachineLearningServicesWorkspacesClientUpdateOperationStatusHeaders::retry_after) - retry-after
    ///
    /// [`MachineLearningServicesWorkspacesClientUpdateOperationStatusHeaders`]: crate::generated::models::MachineLearningServicesWorkspacesClientUpdateOperationStatusHeaders
    #[tracing::function("Microsoft.MachineLearningServices.Workspaces.update")]
    pub fn update(
        &self,
        resource_group_name: &str,
        workspace_name: &str,
        body: RequestContent<WorkspaceUpdateParameters>,
        options: Option<MachineLearningServicesWorkspacesClientUpdateOptions<'_>>,
    ) -> Result<Poller<MachineLearningServicesWorkspacesClientUpdateOperationStatus>> {
        let options = options.unwrap_or_default().into_owned();
        let pipeline = self.pipeline.clone();
        let mut url = self.endpoint.clone();
        let mut path = String::from("/subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/Microsoft.MachineLearningServices/workspaces/{workspaceName}");
        path = path.replace("{resourceGroupName}", resource_group_name);
        path = path.replace("{subscriptionId}", &self.subscription_id);
        path = path.replace("{workspaceName}", workspace_name);
        url.append_path(&path);
        let mut query_builder = url.query_builder();
        query_builder.set_pair("api-version", &self.api_version);
        query_builder.build();
        let api_version = self.api_version.clone();
        Ok(Poller::new(
            move |poller_state: PollerState, poller_options| {
                let (mut request, continuation) = match poller_state {
                    PollerState::More(continuation) => {
                        let (mut next_link, final_link) = match continuation {
                            PollerContinuation::Links {
                                next_link,
                                final_link,
                            } => (next_link, final_link),
                            _ => {
                                unreachable!()
                            }
                        };
                        let mut query_builder = next_link.query_builder();
                        query_builder.set_pair("api-version", &api_version);
                        query_builder.build();
                        let mut request = Request::new(next_link.clone(), Method::Get);
                        request.insert_header("accept", "application/json");
                        request.insert_header("content-type", "application/json");
                        (
                            request,
                            PollerContinuation::Links {
                                next_link,
                                final_link,
                            },
                        )
                    }
                    PollerState::Initial => {
                        let mut request = Request::new(url.clone(), Method::Patch);
                        request.insert_header("accept", "application/json");
                        request.insert_header("content-type", "application/json");
                        request.set_body(body.clone());
                        (
                            request,
                            PollerContinuation::Links {
                                next_link: url.clone(),
                                final_link: None,
                            },
                        )
                    }
                };
                let ctx = poller_options.context.clone();
                let pipeline = pipeline.clone();
                let original_url = url.clone();
                Box::pin(async move {
                    let rsp = pipeline
                        .send(
                            &ctx,
                            &mut request,
                            Some(PipelineSendOptions {
                                check_success: CheckSuccessOptions {
                                    success_codes: &[200, 202],
                                },
                                ..Default::default()
                            }),
                        )
                        .await?;
                    let (status, headers, mut body) = rsp.deconstruct();
                    let continuation = if let Some(operation_location) =
                        headers.get_optional_string(&HeaderName::from_static("location"))
                    {
                        let next_link = Url::parse(&operation_location)?;
                        match continuation {
                            PollerContinuation::Links { final_link, .. } => {
                                PollerContinuation::Links {
                                    next_link,
                                    final_link,
                                }
                            }
                            _ => {
                                unreachable!()
                            }
                        }
                    } else {
                        continuation
                    };
                    let next_link = match &continuation {
                        PollerContinuation::Links { next_link, .. } => next_link,
                        _ => {
                            unreachable!()
                        }
                    };
                    let mut final_body = None;
                    if status == StatusCode::Ok && next_link.as_str() == original_url.as_str() {
                        final_body = Some(body);
                        body = azure_core::http::response::ResponseBody::from_bytes(
                            "{\"status\":\"Succeeded\"}",
                        );
                    }
                    let retry_after = get_retry_after(
                        &headers,
                        &[X_MS_RETRY_AFTER_MS, RETRY_AFTER_MS, RETRY_AFTER],
                        &poller_options,
                    );
                    let res: MachineLearningServicesWorkspacesClientUpdateOperationStatus =
                        json::from_json(&body)?;
                    let mut final_rsp: Option<RawResponse> = None;
                    if res.status() == PollerStatus::Succeeded {
                        final_rsp = Some(RawResponse::from_bytes(
                            status,
                            headers.clone(),
                            if let Some(final_body) = final_body {
                                final_body
                            } else {
                                body.clone()
                            },
                        ));
                    }
                    let rsp = RawResponse::from_bytes(status, headers, body).into();
                    Ok(match res.status() {
                        PollerStatus::InProgress => PollerResult::InProgress {
                            response: rsp,
                            retry_after,
                            continuation,
                        },
                        PollerStatus::Succeeded => PollerResult::Succeeded {
                            response: rsp,
                            target: Box::new(move || {
                                Box::pin(async move {
                                    Ok(final_rsp
                                        .ok_or_else(|| {
                                            Error::new(ErrorKind::Other, "missing final response")
                                        })?
                                        .into())
                                })
                            }),
                        },
                        _ => PollerResult::Done { response: rsp },
                    })
                })
            },
            Some(options.method_options),
        ))
    }
}
